# Packaged LAN acceptance

## Current candidate

Candidate `LAN-20260908-04`, based on `bcec5a3e` plus its captured build inputs, is the current packaged artifact. Build/cook/stage and a natural headless two-round match passed their recorded scopes. It contains the phase-lifecycle, bot-ownership, and footstep fixes. Rendered LAN acceptance is next. Candidate 0803's two-process manual rematch and shared frontend-return results remain historical evidence for their covered changes. See `SLICE_STATUS.md` for the next action.

## Candidate LAN-20260907-01 history

- Candidate: `LAN-20260907-01`, Win64 Development, `LyraGame`, project base `332ff649` plus the recorded source patch. Includes the bot unpossession cleanup and expanded task diagnostics, plus the previously saved duplicate-HUD registration fix. Editor compilation passed after restoring three explicit JSON key conversions in the existing bridge edits. Build/cook/stage passed in 198.16 seconds, AutomationTool exit 0. Exact project/engine revisions, tracked patches, statuses, stage inventory and hashes: `Saved/Logs/codex-packaged-LAN-20260907-01-*`. Untracked source contents are not captured by those patches.
- Stage: `Saved/StagedBuilds/LAN-20260907-01/Windows/LyraGame.exe`; actual executable under `Windows/ProjectB/Binaries/Win64/LyraGame.exe`. Pipeline script: `Saved/package-LAN-20260907-01.ps1`. Existing cook roots and local symbols retained; no deployment or clean-machine validation.
- Offline headless packaged regression passed three natural goals, two round resets, and PostMatch (3–0) at 02:55:22 UTC on September 8 / September 7 local. The test entered Dorado directly with CaptureTheRelic experience, seven bots, selection skipped, three points to win, 90-second rounds, five-second warmup and three-second post-round. No synthetic scoring or movement commands. `-testexit` terminated the process after the real PostMatch log, so this is not results-screen or graceful frontend-return evidence. Log: `Saved/Logs/codex-packaged01-bots-headless-20260907.log`.
- Interactive frontend launched, but the new executable path raised a Windows Security firewall prompt. Computer Use lists PickerHost with no targetable window. Automatic review rejected the attempted **Show more** inspection click because it could not verify the button effect or permission scope. No firewall permission was changed. The visible package remains at that prompt; user handling is needed before interactive LAN verification can continue. The offline run does not validate this route or any client replication.
- Next: clear the visible prompt, host the LAN playlist, confirm moving bots and the single HUD, then complete the remaining multiplayer gates below. The native diagnostic command now also reports task component identity, live movement-task state and path-update postponement.

### Packaged bot movement reproduction and repair

In candidate `-08`, a fresh interactive LAN fixture plus physical Argus selection/lock reached Dorado. All seven bots stayed at spawn. `bway.Debug.RelicBots` reported valid non-partial paths, projected endpoints, correct goal/team Blackboard values, walking mode and speed 600, but idle path following while `Move To[13]` remained active. Gameplay task priority queues on the live pawns were empty. Evidence: `Saved/Logs/codex-packaged08-bot-task-reproduced-20260907.log`. No full match passed in that run.

The controller intentionally disabled automatic brain cleanup on unpossession. Unreal tears down the outgoing pawn's gameplay tasks, while `StartTree` skips an already-started tree. Source now cleans the brain before detaching a pawn, allowing `OnPossess` to start a fresh tree and movement tasks against the replacement pawn. This applies to warmup/round replacement and death/respawn, not only a timed round. Native/editor build: `Saved/Logs/codex-bot-possession-build-fixed-20260907.log`, exit 0. The offline packaged match above verifies natural scoring across round replacement; physical LAN, death/respawn, and disconnect/travel regression remain open.

The control run used the old `-08` executable with the same map, experience, bot count and phase settings. Its first round reached the full 90-second timeout with the relic at midfield, no winner, and no goal. The rebuilt candidate scored at 02:54:17, 02:54:40 and 02:55:19 UTC, then reached PostMatch. Both runs used the native `-testexit` log trigger to end the process at their intended checkpoint (first PostRound for control; PostMatch for the repaired build). Control log: `Saved/Logs/codex-packaged08-bots-headless-control-20260907.log`; compact comparison: `Saved/Logs/codex-bot-package-comparison-20260907.json`. This is an observed before/after regression, not a soak test or a network proof. BF-092 records prevention; BF-046 records the recurring bridge conversions.

## Previous candidate (-08)

- Candidate: `LAN-20260906-08`, Win64 Development, `LyraGame`, project base `fb76cbdf`; includes the frontend Breakaway HUD reference and `bway.Debug.RelicBots`. Build/cook/stage passed with AutomationTool exit 0 in 154.92 seconds. A transient Zen connection failure recovered during staging. Log/exit status: `Saved/Logs/codex-lan-package-20260906-08*`; source/engine identity, patches, status, and complete staged-file SHA-256/size inventory: `Saved/Logs/codex-packaged-LAN-20260906-08-*`.
- Stage: `Saved/StagedBuilds/LAN-20260906-08/Windows/LyraGame.exe`; runtime log: `Saved/Logs/codex-packaged08-bot-host.log`. Launched through the existing LAN test fixture. Interactive diagnostics and HUD verification are pending. Windows Security displayed a firewall prompt; despite user authorization, the computer-use tool exposes PickerHost with an empty windows list and rejects game-targeted clicks over that separate dialog. User assistance was requested for this tool limitation, not for renewed permission.
- Next: run `bway.Debug.RelicBots` on the active packaged host, inspect the six-slot HUD, and continue the complete multiplayer match/front-end acceptance sequence. No candidate -08 runtime acceptance is claimed yet.

## Previous candidate (-07)

### Candidate -08 visible HUD and duplicate registrations

At 16:11 UTC, a screenshot of the running -08 host showed the six-slot ability bar with icons and key labels beneath the still-open firewall dialog. This confirms the Breakaway HUD replacement is active in the packaged frontend match; it does not test ability activation, cooldowns, or client replication. The same view showed duplicate score/timer displays.

Native cold property inspection found direct CaptureTheRelic experience entries for `W_BW_CaptureTheRelic_ScoreWidget` at `HUD.Slot.TeamScore` and `W_BW_RelicStatusWidget` at `HUD.Slot.ModeStatus`. `LAS_BW_MatchHUD` and `EAS_BW_CaptureTheRelic` already supply those same entries. The development experience's direct list was empty. Source now clears the two duplicate entries from `GameFeatureAction_AddWidgets_1`, preserving the shared action sets. The asset compiled/saved; a separate cold commandlet exited 0 and confirmed the direct list empty with one shared entry per widget. Evidence: `codex-duplicate-score-hud-roots.log`, `codex-duplicate-match-widgets-repair-v3.log`, and `codex-duplicate-match-widgets-cold-verify.log` under `Saved/Logs`. Earlier attempts stopped without edits because the widget-entry fields are not exposed to Python. The fix is not in -08; packaged visual verification remains open.

- Candidate: `LAN-20260906-07`, Win64 Development, `LyraGame`, project base `3c6fd82b`; includes the weak-reference round-cleanup repair. Build/cook/stage passed with AutomationTool exit 0 in 145.98 seconds. Pipeline log and exit status: `Saved/Logs/codex-lan-package-20260906-07*`. Source/engine HEAD, status, binary patches, and complete staged-file SHA-256/size inventory: `Saved/Logs/codex-packaged-LAN-20260906-07-*` (untracked contents are not preserved by patches).
- Stage: `Saved/StagedBuilds/LAN-20260906-07/Windows/LyraGame.exe`. Cold startup and the existing `bway.Test.HostLAN` fixture reached hero selection and Dorado. No client has joined this candidate yet: a Windows Security firewall prompt blocked UI work and was handed to the user. Host logs reached round two at 15:45:13 UTC, but without bot replacement this does **not** validate the crash repair. Runtime log: `Saved/Logs/codex-packaged07-lifetime-host.log`.
- Acceptance remains open: repeat late join during an active round, verify bot removal and round cleanup after collection, then complete the multiplayer and frontend gates. This candidate predates the HUD reference repair described next.

### Bot diagnostic command

Candidate -07 late-join regression passed the observed round-reset scenario at 16:01–16:02 UTC. After the firewall prompt cleared, a second packaged process connected directly to localhost during round 12. Server logs record Bot 7 removal at 16:01:25 and successful join. The host started round 13 at 16:02:39; the client received RoundComplete and then RoundActive, and both processes remained alive. No forced garbage collection or complete-match pass is claimed. Evidence: `Saved/Logs/codex-packaged07-lifetime-host.log` and `codex-packaged07-lifetime-client.log`. This is a direct-connection regression, not a new frontend discovery pass. The user explicitly authorized handling verified Unreal build security prompts; that authorization is now recorded in the testing ladder and installed computer-use guidance.

Source adds the non-Shipping console command `bway.Debug.RelicBots`. Run it on the server to capture the current world/net mode, active relic, each bot's pawn/velocity/movement mode and speed, active Behavior Tree tasks, relevant Blackboard values, selected agent NavData, default-extent endpoint projections, and a synchronous path result to the relic. It is an on-demand read-only snapshot, with no movement request or Blackboard mutation. Client controllers are server-only, so a zero client count is expected. `LyraEditor` compilation passed (`Saved/Logs/codex-bot-diagnostic-build.log`, 29.11 seconds); live command execution remains unverified and the command is not in candidate -07.

### Frontend experience HUD mismatch

Read-only cold asset inspection found that `B_BW_Experience_CaptureTheRelic`, used by the frontend playlist, referenced `LAS_ShooterGame_StandardHUD`; the development experience referenced `LAS_BW_MatchHUD`. Shooter's action set installs `W_ShooterHUDLayout` and the three-slot `W_QuickBar`, explaining the packaged three EMPTY slots despite the host receiving six tagged hero abilities. The Breakaway action set installs `WBP_BW_MatchHUDLayout`, which provides the intended match HUD slots.

Source now replaces only the CaptureTheRelic experience's Shooter HUD action-set reference with the existing Breakaway HUD action set. The asset compiled and saved in a commandlet; evidence: `Saved/Logs/codex-capture-relic-hud-repair.log`. A separate cold commandlet exited 0 and confirmed the replacement persisted (`codex-capture-relic-hud-cold-verify.log`, 15:45:49 UTC). Packaged visual verification remains open. The experience's Shooter standard components also differ from the development experience (quickbar, damage-number and character nameplate targets); those references have not been changed. The late joiner's missing selected hero remains a separate issue.

## Previous candidate (-06)

### Saved navigation investigation (September 6, 15:48–15:56 UTC)

The original committed Dorado map passes all twelve spawn-to-midfield path queries after both endpoints are projected onto navigation: 12 valid, non-partial paths, 0 invalid. Midfield projects to Z=30; spawn points project to approximately Z=240–249. Evidence: `Saved/Logs/codex-dorado-nav-original-projected-paths.log`. This is cold source-asset navigation evidence, not cooked navigation or live bot movement evidence.

The first diagnostic incorrectly targeted midfield at Z=350 and returned no paths. A Python rebuild attempt and a native `ResavePackages -BuildNavigationData` attempt also encountered `AsyncLoadLock` (0x20). The native command returned exit 0 despite reporting that navigation was not built. Engine source shows the lock waits for editor ticker callbacks. A command-only `-ini:Engine:[/Script/NavigationSystem.NavigationSystemV1]:bWaitForAsyncLoadingBeforeBuildingNavigationAutomatically=False` override allowed the dedicated commandlet to finish its mesh compilation, build static navigation, and save Dorado. Corrected projected queries passed in both rebuilt and original maps. The experimental map change was restored to HEAD; the rebuilt copy is retained under `Saved/L_BW_Dorado_nav_rebuild_verified.umap`. No navigation settings or source map changes are retained.

Next bot diagnostic: inspect the actual packaged server's loaded NavData, projected endpoints, path-following result, and Blackboard/active branch. A running Behavior Tree log alone remains insufficient. The Windows Security dialog still covered candidate -07 during this investigation; no client-replacement crash regression or interactive bot check was performed.

- Candidate: `LAN-20260906-06`, Win64 Development, `LyraGame`, built September 6, 2026. This supersedes `-05` as the packaged candidate.
- Project base: `24e0cd63065ccf9107a35dc9abf816b9aaf871cd`, including bot pursuit, Dorado goal ownership and replication, server-owned rematch travel, results layout, and gameplay HUD suppression during results. Project and engine HEAD, status, and binary patches are retained under `Saved/Logs/codex-packaged-LAN-20260906-06-*`; the patches do not preserve untracked file contents.
- Pipeline: the BuildCookRun command below with staging directory `LAN-20260906-06`. Build, cook, and stage passed; AutomationTool exited 0 after 140.06 seconds. The full log is `Saved/Logs/codex-lan-package-20260906-06.log`, with exit status in the corresponding `-exit.txt` file. Existing gameplay-tag and asset migration warnings remain.
- Artifact: `Saved/StagedBuilds/LAN-20260906-06/Windows/LyraGame.exe`; the actual game executable is `Windows/ProjectB/Binaries/Win64/LyraGame.exe` beneath the candidate directory. The complete staged-file SHA-256 and size inventory is `Saved/Logs/codex-packaged-LAN-20260906-06-sha256.csv`. Symbols remain in this local Development stage; no release archive or clean-device deployment has been performed.
- Acceptance: **failed runtime gate**. Physical frontend host/find/join passed with two packaged processes on one PC, but the host crashed during the first round reset after a joining human replaced a bot. See the crash diagnosis and source repair below. Full match, results, rematch, four humans, and second-PC LAN acceptance remain open.

### Candidate -06 runtime failure and source repair

Physical frontend testing on September 6 at 15:29–15:34 UTC passed cold startup, Play Lyra -> Start a Game -> LAN -> Local (listen server + bots), then a second packaged process's Browse discovery and join. The client joined Dorado during round one at 15:32:31; the server removed Bot 7 to retain eight players. This is two processes on one PC, not the four-human or second-PC gate. Host selection timed out normally before the client joined.

Both visible gameplay HUDs showed empty ability slots and the waiting-for-players banner. The joining player's server log reports no selected hero, while the host received Korryn's six tagged abilities. These are distinct unresolved symptoms. Bots stayed near their spawns in the observed views, and round one expired without a score; packaged objective pursuit remains unverified.

At 15:33:37 the host crashed during the round-two transition. The exact staged executable/PDB and minidump resolve the failure to `RemovePassiveGoldIncome -> GetAbilitySystemComponentFromActor -> GetInterfaceAddress`, reading reclaimed memory. `PassiveGoldEffectHandles` held unreflected `TObjectPtr` keys after the replaced bot's PlayerState was destroyed. Source now uses weak keys for this map and the neighboring non-owning respawn timer cache. `LyraEditor` compilation passed in 28.94 seconds (`Saved/Logs/codex-passive-gold-lifetime-build.log`). The fix is not in candidate -06 and needs the same packaged join/replacement/round-reset regression before runtime acceptance.

Evidence: `Saved/Logs/codex-packaged06-frontend-host.log`, `codex-packaged06-frontend-client.log`, and `codex-packaged06-round-reset-crash-stack.log`. The dump and CrashContext remain beneath candidate -06's `Windows/ProjectB/Saved/Crashes/UECC-Windows-3EC825294629116E567561A51D27A7B0_0000`. The client timed out after 60 seconds and returned to the frontend; its process was then closed. No complete match or rematch pass is claimed.

## Previous candidate (-05)

- Candidate: `LAN-20260906-05`, Win64 Development, `LyraGame`; built September 6, 2026 at approximately 06:13 UTC.
- Project base: `6cfdbe75c303ab6df3046576d096eb864ff6a6da`, including the selection ownership, roster, and layout fixes. The uncommitted PostMatch return guard and other tracked differences are captured in `Saved/Logs/codex-packaged-LAN-20260906-05-project.patch`.
- Engine baseline: the same source engine listed below, with tracked changes and status captured in `codex-packaged-LAN-20260906-05-engine.patch` and `codex-packaged-LAN-20260906-05-engine-status.txt` under `Saved/Logs`. Untracked engine file contents are not captured by the patch.
- Pipeline: the BuildCookRun command below with staging directory changed to `LAN-20260906-05`. Build, cook, and stage passed; AutomationTool exited with code 0 after 145 seconds. A transient Zen connection failure recovered during staging.
- Artifact: `Saved/StagedBuilds/LAN-20260906-05/Windows/LyraGame.exe`; SHA-256 inventory: `Saved/Logs/codex-packaged-LAN-20260906-05-sha256.csv`; pipeline log: `Saved/Logs/codex-lan-package-20260906-05.log`.
- Acceptance: frontend launched. Interactive keyboard verification is pending because a Windows Security network permission dialog covers the game and cannot be targeted by the available UI automation. No four-player packaged or full-match pass is claimed.

### Keyboard investigation

The hero card is a focusable CommonButtonBase with no triggering input action and no hold requirement. Engine `CommonButtonTypes.cpp` suppresses Slate Accept in `SCommonButton::OnKeyDown` and `OnKeyUp` when `CommonButtonAcceptKeyHandling` is `Ignore`. The engine constructor defaults to `Ignore`, and no project or engine config override was found. `TriggerClick` is the engine-supported alternative. A packaged comparison and frontend regression check remain required before accepting that configuration change; the candidate above has no such override.

The source config now explicitly sets `CommonButtonAcceptKeyHandling=TriggerClick`. This is not included in candidate `-05`. Verify physical Enter and Space selection, navigation between cards, lock activation, and frontend navigation after cold startup. Also check that existing action-bound buttons do not activate twice and that gamepad acceptance still works. Source inspection alone does not establish those passes.

Cold-load verification passed at 12:22 UTC on September 6. An unattended `UnrealEditor-Cmd -run=pythonscript -unattended -nullrhi` run exited with code 0; native `getall CommonUISettings CommonButtonAcceptKeyHandling SHOWDEFAULTS` reported `TriggerClick` on both the class default and transient settings objects. Evidence: `Saved/Logs/codex-commonui-cold-config-native.log`. The first inspection script failed because the property was not exposed through the Python property API; its failure is retained in `codex-commonui-cold-config.log`. This verifies config loading, not physical input. The commandlet required source-engine and user-cache access; the earlier sandboxed interactive editor remained stalled at Zen data-path validation.

### Unattended match evidence

The hidden candidate `-05` listen-server process continued running after the attempted window close. At 12:17 UTC it reached round 230; at 12:19 UTC that round again expired with the relic on midfield and no winner. Recent rounds repeat this pattern while the bot controller logs report a running behavior tree. This does not establish why bots fail to progress the objective, nor justify changing draw rules. Investigate navigation, blackboard state, and task execution before claiming a complete unattended match. Evidence excerpt: `Saved/Logs/codex-packaged05-unattended-round230-evidence.log`; original: `codex-packaged05-keyboard-baseline.log`. A successful CloseMainWindow return alone must not be treated as process termination.

## Previous candidate (-04)

### Goal ownership repair (September 6, 14:10–14:13 UTC)

The suspected goal-class mismatch was disproved: B_Bway_Goal derives from BwayGoalVolume. Both Dorado goal spawn points instead had TeamIndex=-1, which the spawn data copied into OwningTeam. The map now sets SP_GoalTeam1 to 0 and SP_GoalTeam2 to 1, matching their existing labels and goal-spawn assets; the relic spawn remains neutral. The map was saved explicitly as a .umap.

Fresh four-player PIE verified opposite goal locations in all four server bot Blackboards. At 14:11:36 a bot scored in the opposing goal, Team 1 received one point, and round two started. No scripted movement, pickup, or scoring calls were used. Round two ended through the territory timeout rule, awarding Team 2 a point; round three started. Full match completion and round-reset bot behavior remain unverified. Client goal ownership still uses the nonreplicated default and needs repair. Evidence: `Saved/Logs/codex-dorado-goal-team-scoring-verified.log`. PIE was stopped.

Inspection hygiene: the earlier pursuit run's PIE teardown reported references retained by top-level Python inspection variables. Subsequent inspections use function-local runtime references and delete the helper after returning. Treat those earlier teardown diagnostics as inspection contamination, not unexplained gameplay crashes.

### Pursuit repair and runtime evidence (September 6, 14:05–14:07 UTC)

After the stalled editor exited, an interactive editor launch with engine/cache access restored the bridge. The authored graph's existing Free Relic selector was reconnected to the root and saved, preserving its existing Move To RelicLocation task. In a fresh four-player Dorado PIE session, selection timed out normally, gameplay started, all four bots moved from spawn to midfield, and a bot acquired the relic through existing proximity/request behavior. This establishes movement and pickup after the reconnect; a new explicit pickup sequence was not necessary for this observed path.

Scoring remains blocked: all four bot Blackboards reported an unset EnemyGoalLocation (MAX_FLT vector), while the server contained two B_Bway_Goal actors at Y=-4000 and Y=4000. The bot helper currently searches for ABwayGoalVolume. Investigate the live goal class and its team contract before changing lookup. This run does not establish scoring, possession-change interruption, full-match completion, or packaged behavior. PIE was stopped. Evidence: `Saved/Logs/codex-bot-pursuit-reconnected-verified.log`.

### Bot asset diagnosis (September 6, 12:24 UTC)

The unattended read-only asset inspection exited with code 0. `BT_BW_RelicBot` uses `BB_BW_RelicBot`; its key names and types match the native service, and the root selector has the update service attached. However, the reachable root children are carrier, enemy-carrier chase, and a `RelicLocation Is Set` branch whose composite (`BTComposite_Selector_0`) has **no children**. The `Move To RelicLocation` tasks (`BTTask_MoveTo_2` and `_3`) are not reachable from the root. This is a concrete asset wiring defect that prevents the tree from pursuing an initially free relic; a successful `RunBehaviorTree` log does not validate it. All inspected Blackboard decorators also have observer aborts disabled.

Repair the authored graph with a reachable free-relic sequence: availability condition, move to relic, then pickup. Configure possession-change aborts deliberately and verify the serialized tree after reopening, then prove server-side movement, pickup, scoring, and match completion. Do not patch only the runtime `Children` array while leaving the editor graph inconsistent. Dorado contains both a NavMeshBoundsVolume and RecastNavMesh, but actor presence does not prove usable cooked paths. Evidence: `Saved/Logs/codex-relic-bot-asset-inspection.log`; inspection script: `Saved/inspect_relic_bot_assets.py`. No asset mutation or runtime navigation pass was performed.

- Project base: `e405d746` (September 6, 2026), plus the hero feature type registration in `DefaultGame.ini` and the uncommitted `Server_RequestReturnToFrontEnd` PostMatch guard. The exact source delta is saved in `Saved/Logs/codex-packaged-LAN-20260906-04-project.patch`; this candidate is not a clean-commit release.
- Engine: local UE 5.8.1 source tree, `E:/Github/UnrealEngine`, HEAD `71fe36aac5a8df5ccd66c763ffc902b29b6a9c43`. Local engine modifications must also be captured before accepting the artifact; HEAD alone does not establish reproducibility.
- Platform/configuration: Win64 Development, `LyraGame`, listen server and clients.
- Pipeline: AutomationTool BuildCookRun, build, cook by the book, stage, Pak.
- Stage: `Saved/StagedBuilds/LAN-20260906-04/Windows`; launch `LyraGame.exe`. The actual game binary is `ProjectB/Binaries/Win64/LyraGame.exe`. No release archive or deployment yet.
- Logs: `Saved/Logs/codex-lan-package-20260906-feature-types-final.log` and the referenced UBT log.
- SHA-256 inventory: `Saved/Logs/codex-packaged-LAN-20260906-04-sha256.csv`, covering every staged file, including binaries, Pak/IoStore files, symbols, configuration, and manifests. The earlier `-02` inventory is retained separately.
- Existing cook roots: frontend, DevMap, Dorado, and the configured BreakawayCore content directory. No new broad cook inclusion was added.
- Status: build, cook, and stage passed with AutomationTool exit code 0. Candidate `-04` passed cold-load hero registration, activation, manual selection/lock, and travel checks. Two-process frontend host/discover/join and shop smoke checks passed on candidate `-02`; they have not been repeated on `-04`. Full multiplayer acceptance remains open.

The first cook reported a missing `Alona` asset domain. Engine `AssetReferencingDomains.cpp` derives plugin domains from `IPlugin::GetName`; the registered plugin is `Hero_Alona`. The corrected identifier passed the fresh cook. BF-069 records prevention. Engine tracked changes and status were captured in `Saved/Logs/codex-package-engine-tracked.patch` and `codex-package-engine-status.txt`; untracked engine content is not included in that patch.

Command issued with the editor closed:

```powershell
& 'E:/Github/UnrealEngine/Engine/Build/BatchFiles/RunUAT.bat' `
  '-ScriptsForProject=E:/Unreal Projects/ProjectB/ProjectB.uproject' `
  BuildCookRun '-project=E:/Unreal Projects/ProjectB/ProjectB.uproject' `
  -target=LyraGame -platform=Win64 -clientconfig=Development `
  -build -cook -stage -pak -utf8output -unattended `
  '-stagingdirectory=E:/Unreal Projects/ProjectB/Saved/StagedBuilds/LAN-20260906-04'
```

## Cook repair evidence (September 6)

The first cook exited with code 25 after the standalone build passed. Its terminal errors were the stale Alona domain, incompatible TopDownArena movement override, and failed relic redirector import. The domain correction is committed separately. A full asset-registry referencer scan established that `B_Bway_Ball_Interactable` had no referencers and that `BP_Bway_Relic` was referenced only by that obsolete redirector; both packages contained only redirectors leading to a deleted asset. Those two dead packages were removed. The live relic remains `/BreakawayCore/GameModes/BallMode/Relic/BP_BW_RelicActor`, referenced by game state, spawn data, and the grant pad.

The shared hero parent now requires BwayCharacterMovementComponent. TopDownArenaMovementComponent derives from that class and declares its BreakawayCore module/plugin dependency, preserving its existing speed override. The editor build passed (`Saved/Logs/codex-cook-asset-repair-build.log`). After restart, B_Hero_Arena compiled, its movement default object had the correct class, the live relic loaded, and the removed redirectors were absent from the registry (`Saved/Logs/codex-cook-assets-editor-verified.log`). BF-072 records prevention. The subsequent fresh cook and stage passed; content warnings still require review.

## Packaged smoke evidence (September 6, 04:22–04:32 UTC)

The editor was closed. Candidate `-02` tests used two separate packaged game processes on the source machine, controlled through visible menus with Computer Use.

- Host: **Play Lyra → Start a game → Network: LAN → Local / Listen server + bots** created a session and opened hero selection.
- Client: **Play Lyra → Browse → Refresh search** discovered that session with 1/8 players and 63 ms ping. Selecting its row joined hero selection. Both peers travelled into Dorado after selection timed out. This does not verify manual hero locking; the attempted hero click overlapped timeout.
- The host log reports a final bot target and count of six with the two humans. Team balance still needs an explicit roster check.
- Client **B** opened the native upgrade shop. At 59 gold, the 75-gold purchase was disabled. In round two, a physical button click produced **Purchasing…**, followed by **Purchase complete**, owned attack rank 1/4, and upgrades 1/4. Passive gold continued during the observations, so screenshots alone do not establish the exact debit.
- Evidence: `Saved/Logs/codex-packaged-host2-smoke.log` and `codex-packaged-client1-smoke.log`. Earlier single-host run: `codex-packaged-frontend-smoke.log`.

Polish failures are visible: Lyra/sample and development menu entries, clipped labels, hero-selection TODO text and blank panels, duplicate neutral HUD labels, an in-match “Waiting for additional players” banner, floating hero mesh alignment, and missing Alona animation setup. Startup also records unprecached PSO hitches and invalid `BwayGameFeatureData` primary asset IDs. These are open issues, not acceptance passes.

Initial overlapping game windows exhibited button nonresponse while console input worked. Closing the first process restored the remaining menu. Relaunching the other window at a separate position allowed both menus and the shop to work. Keep windows separated for automation; the cause is not yet established as a game defect.

## Hero feature registration verification (04:38–04:39 UTC)

The hero definitions report primary type `BwayGameFeatureData`. `UAssetManager::RegisterSpecificPrimaryAsset` rejects unknown types, while project settings previously registered only `GameFeatureData`. The fix registers the existing subclass type with four explicit asset paths and the engine GameFeatureData base class. IDs and gameplay state remain unchanged; the Game Features subsystem continues to own activation and unloading.

Candidate `-04` passed a fresh build/cook/stage. A cold packaged launch with `AssetManager.DumpTypeSummary,bway.Test.HostLAN` reported exactly four feature definitions. All four hero plugins reached Active after travel, with no invalid BwayGameFeatureData ID warnings or scan conflicts. The host command is a native fixture, not frontend click evidence. Physical clicks then selected Korryn and locked the selection; logs confirm Hexweaver data on the human PlayerState and Korryn on the spawned match pawn. Evidence: `Saved/Logs/codex-packaged-feature-types-verified.log`. BF-073 records prevention.

The first scan attempt used broad hero roots and encountered ignored conflicts with unrelated primary data types; explicit paths removed those conflicts. Zen briefly disconnected during staging and the existing retry recovered; both staging attempts ultimately exited successfully.

## Hero selection follow-up (source editor, 05:14–05:22 UTC)

These changes are newer than packaged candidate `-04`. Two same-process listen-server PIE worlds reproduced two client faults: selection-active state was authority-only, and the screen cached a null PlayerState before replication arrived. A physical hero click consequently used the offline frontend route and never reached the server.

The manager now replicates its active flag. The screen reacquires its owning PlayerState while active and before requests, binds delegates uniquely, and keeps a locked client visible until the server advances the phase. After rebuilding/restarting, physical Korryn selection and lock updated both the authoritative and client PlayerStates; the client screen remained active while the host was unready. Evidence: `Saved/Logs/codex-selection-client-lock-verified.log`. Auto-lock was disabled only on the transient server component to keep the inspection window open. This test does not prove timeout behavior, staging travel, or packaged acceptance.

The selection layout now has separate hero and details columns, with obsolete level/TODO/empty panels removed and native data-driven stats text bound. UMG compiler GUID metadata was repaired, force-saved, reloaded, and verified across editor restarts. Portraits now fit a fixed-size container independently of source texture dimensions. Remaining presentation issues include sparse class-based card spacing, timer/readiness formatting, and duplicate host selection widgets.

The ability slot configuration previously deserialized as one invalid entry. Correct array entries with `TagName` now resolve all six slots and six authored ability descriptions for each of the four heroes. Final PIE physical selection/lock again passed with the full Korryn portrait and scrollable stats/ability descriptions visible. Both server and client reported Korryn locked while the unready host kept the selection phase active. Evidence: `Saved/Logs/codex-selection-final-verified.log`; no Blueprint runtime errors, Accessed None, or ensure failures appear in that final log. Native build: `Saved/Logs/codex-selection-hydration-final-build.log`, exit 0. PIE stopped and temporary client window/background-throttle settings restored afterward. Packaged ability-bar regression testing remains open because the shared slot configuration also affects match UI.

## Four-player selection ownership and roster verification (06:02–06:03 UTC)

Source-editor follow-up, still newer than packaged candidate `-04`:

- Removed duplicate viewport creation from the phase Blueprint and staging level Blueprint. The owning PlayerController now pushes the single CommonUI menu, including for joining players. The widget declares menu input mode and reacquires GameState/selection-manager references when replication arrives after creation.
- Connected active selection registration/removal to GameState player lifecycle. Before the fix, joining humans were absent from selection while their replaced bots remained registered. Team changes now refresh the selection entry; removal defers readiness evaluation until the current player/bot replacement finishes.
- Hero cards use two compact columns. The countdown displays whole seconds, and readiness reads `N / 8 ready` using the existing presentation timer instead of Blueprint Tick. An initial focus target is configured, but keyboard activation did not select a hero in this test and remains unresolved; gamepad acceptance is unverified.
- Four same-process PIE worlds each had exactly one active selection screen, eight live players, and an exactly matching selection roster. Four bots were ready before any human locked. Physical mouse input selected Argus on the host, Rawlins on client 1, Korryn on client 2, and Alona on client 3. Readiness visibly advanced as clients locked. After the final host lock, every world reported selection inactive, eight ready, an exact roster match, and zero active selection screens. Server and owning-client PlayerStates agreed on locked hero IDs.

Evidence: `Saved/Logs/codex-selection-four-player-verified.log`. Builds `codex-selection-roster-build.log` and `codex-selection-menu-input-build.log` exited 0. No Blueprint runtime error, Accessed None, or ensure failure was found in the final log. One read-only inspection used the wrong Python controller method and was corrected; that tooling error is recorded separately from gameplay. Direct staging entry also attempts relic spawning without arena spawn points and leaves the players in the staging world after selection; this run is not travel, gameplay, separate-process, or packaged-flow evidence. Auto-lock was disabled only on the transient server instance. PIE stopped and test settings were restored afterward.

Level-Blueprint editor-tool caveat: the generic Blueprint mutation tool saved a `.uasset` beside the `.umap`. The level was explicitly saved with `EditorLoadingAndSavingUtils.save_map`, the generated duplicate was removed, and a fresh editor reload confirmed the saved map no longer contains the old BeginPlay UI entry. Human disconnect/rejoin, deferred readiness after disconnect, keyboard/gamepad activation, and packaged regression remain open.

## Goal ownership and complete PIE match (September 6, 14:18–14:22 UTC)

Goal ownership now replicates through `OwningTeam`, with an OnRep color refresh. Spawn initialization uses an authority setter that also refreshes the server material after BeginPlay. Previously clients retained default team 0, and post-spawn ownership assignment left the server color stale. The editor build passed (`Saved/Logs/codex-goal-ownership-build.log`, exit 0).

After a cold editor restart, four same-process PIE worlds on Dorado each reported goal ownership `(-4000, 1), (4000, 0)` by Y position. The server and all three clients matched. No synthetic scoring or bot movement requests were used. Bots scored five natural goals, with round winners 1, 1, 2, 2, 2; the match reached PostMatch at 14:21:54 UTC with Team 2 winning 3–2. All four peers logged the same five-round result. Bots therefore resumed successfully across four round resets in this run. PIE stopped afterward.

Evidence: `Saved/Logs/codex-goal-ownership-full-match-verified.log`. This is replicated state and log evidence in same-process PIE, not packaged LAN, physical frontend navigation, visual goal-color acceptance, late joining, or second-match reset evidence. The previous idle-bot observation alone does not establish a round-reset defect; this completed run supersedes that suspicion. Packaged candidate `-05` predates these fixes.

## Results return and second-session PIE smoke (September 6, 14:23–14:30 UTC)

A fresh four-world PIE match completed naturally, 3–0 in three rounds. Physical input on client 1's **Return to Lobby** button returned all four worlds to `L_LyraFrontEnd`; all four had zero relic bot controllers. The former host and client visibly displayed usable frontend menus. The existing PostMatch return guard allowed this valid request; rejection outside PostMatch remains untested.

From the returned host, physical **Play Lyra → Start a game → Network: LAN → Local / Listen server + bots** created a new session. The returned client used **Play Lyra → Browse**, discovered the new 1/8 session at 20 ms, and joined its row. Both travelled to Dorado after selection timeout. Authority had eight players and six bots; the client had the same eight-player count. The two unused peers remained in their independent frontends. The new match displayed 0–0 and started round 1. At 14:30:03 UTC, all sixteen server/client PlayerState copies reported objective score 0 and match gold earned 36, approximately 36 seconds after round start. This supports fresh scoring/economy state in this run; no purchased upgrades or placed buildables were carried through this test, so their reset is unproven.

Evidence: `Saved/Logs/codex-results-return-second-session-verified.log`. This extends same-process PIE evidence only. Four separate processes, packaged travel, full second-match completion, and clean-machine LAN remain open. The results table visibly overlaps names and columns at the 859×520 client window size. Enter after Tab did not select a hero in fresh client selection; the CommonUI config change alone has not resolved verified keyboard acceptance. Play Again still uses local level reload/restart and needs a multiplayer implementation.

## Rematch implementation in progress (September 6, 14:35–14:48 UTC)

Play Again now submits intent through the owning PlayerController to a server PostMatch guard and non-seamless arena reload. Selection is rerun and PlayerStates/world actors are recreated. This replaces local `OpenLevel`/`restartlevel`. A server-side native probe rejected an early rematch. Reflective Python calls on client objects invoke implementations locally: they are **not RPC transport evidence**, including the attempted early-return probes in this run.

Two physical client Play Again clicks after natural one-point test matches reached server travel. They exposed missing listen mode in PIE's world URL, then a port reset from 17777 to 7777 during absolute travel. Retaining `World->URL.Port` did not fix it because that stored value was also 7777. The latest source reads the net driver's actual bound socket port and retains listen mode. Runtime verification of that final correction is pending; rematch has **not passed**. Logs: `codex-rematch-missing-listen-reproduced.log`, `codex-rematch-port-reset-reproduced.log`, and `codex-rematch-world-url-port-reproduced.log` under `Saved/Logs`. The final reproduction used temporary selection/scoring fixtures and a physical client button click; it is not natural full-match evidence. Packaged verification remains open.

## Rematch reconnection verified in PIE (September 6, 14:56–14:59 UTC)

The final implementation uses relative, explicitly non-seamless server travel. It removes inherited selection-skip/transition flags, retains listen mode, and normalizes the travel context's port to the net driver's bound port. Unreal rejects a port embedded in the public ServerTravel URL; relative travel retains it through the context instead. Build `Saved/Logs/codex-rematch-relative-build.log` passed (exit 0).

After a fresh editor restart, a natural goal ended a temporarily configured one-point match. A physical client **Play Again** click reached server travel at 14:58:34 UTC. The host reopened port 17777; all three clients reconnected. At 14:59:03, all four Dorado worlds had eight players and round number 0, with exactly one authority. All 32 PlayerState copies reported match gold earned 0. The client visibly displayed fresh hero selection with 4/8 ready. The host resolved the authored three-point rule again. PIE stopped afterward. Evidence: `Saved/Logs/codex-rematch-relative-verified.log`.

This supersedes the pending reconnection result above. It verifies the physical button, network request, four-peer reconnection, fresh selection, and earned-gold reset in same-process PIE. Previously purchased upgrades, placed buildables, full rematch completion, simultaneous conflicting results requests, separate-process and packaged behavior remain unverified. BF-086 records the travel and test-method pitfalls.

## Results-table layout verification (September 6)

The breakdown panel now uses viewport-relative bounds instead of a fixed 1200×720 canvas slot. Player columns are 180 units wide, with horizontal overflow support. Names use smaller, non-scrolling text with ellipses. The label column reserves the same header height as player columns and uses matching font metrics; an extra 32-unit spacer was removed from player columns. Both widget Blueprints compiled and were saved.

A fresh four-world PIE match, temporarily set to one point, reached results through a natural goal. Visual checks at client 859×520 and host 1286×760 confirmed all eight columns fit, names no longer overlap, stat values align with labels, and both footer controls remain visible. Evidence: tool screenshots in this task and `Saved/Logs/codex-results-layout-verified.log`. No native gameplay code changed. Packaged layout, full-name access for truncated labels, portrait data binding, and suppressing the underlying gameplay HUD remain open. BF-087 records the sizing, row-alignment, and CommonUI scrolling-style causes.

## Post-match HUD suppression (September 6, 15:18–15:20 UTC)

The local results orchestration widget now collapses `UI.Layer.Game` when results begin, remembers its prior visibility through a weak reference, and restores it on destruction. This suppresses gameplay presentation without changing replicated state or destroying HUD widgets. The editor build passed: `Saved/Logs/codex-results-hud-layer-build.log`, exit 0.

After a fresh restart, a four-world PIE match temporarily set to one point ended through a natural goal. All four peers logged local gameplay-layer suppression. The client results screenshot confirmed score/timer, relic labels, health bar, and ability bar were absent behind the table. Physical client **Play Again** returned to selection, then resumed gameplay with the HUD visible again. PIE stopped afterward. Evidence: `Saved/Logs/codex-results-hud-layer-verified.log` and task screenshots. This verifies the observed rematch lifecycle in PIE; packaged behavior and same-world manual results dismissal remain unverified. Debug screen messages are separate from the HUD and remain visible in development.

## Remaining acceptance checks

1. Record terminal build/cook/stage results and first causal errors. Preserve manifests, executable/Pak hashes, effective configuration, and matching symbols for the exact tested output.
2. Launch the staged executable with the editor closed. Confirm the frontend opens without loose project content or a cook server. A source-machine smoke test does not prove clean-machine compatibility.
3. Host LAN through the frontend, discover it from three separate client processes, join, select heroes and teams, and enter Dorado. Verify four humans plus four bots, four players per team, and consistent selection on every peer. Direct-IP joining is supplementary evidence.
4. Exercise movement, attacks, abilities, relic pickup/pass/throw/fumble/score, buildable placement/damage/persistence, base healing, and shop transactions from owning clients. Compare authority and client state; include insufficient funds, duplicate requests, and forbidden purchase windows.
5. Exercise deaths and repeated respawns, upgrade persistence, disconnects, and joining during allowed phases. Verify input, camera, HUD, team counts, and bot replacement after each transition.
6. Complete a best-of-five match through gameplay, covering goal, elimination, and time/territory round outcomes across the test set. Verify round summaries, final results, and gold earned independently of spending.
7. Return each peer to a usable frontend. Host/join a second match and verify fresh match gold, upgrades, stats, scores, buildables, and session state.
8. Repeat the gameplay and transition checks on DevMap. Run a clean second-PC LAN test with required runtimes and firewall configuration, and record startup/dependency failures separately from gameplay failures.

All items remain open until backed by logs and visible behavior from the packaged candidate. Native probes may supplement gameplay but do not prove physical input or complete frontend navigation. Two same-process PIE worlds do not satisfy these gates.

## Hero-card visual-state repair (September 7, 2026)

The current source editor confirms `CommonButtonAcceptKeyHandling=TriggerClick` and focusable hero cards. The keyboard failure therefore remains unresolved by the earlier settings change alone. The Windows Security prompt still blocks physical UI verification, including screenshots of the PIE window.

Inspection found a separate, concrete defect in `WBP_BW_HeroSelect_Button.UpdateVisualState`: all three `SetVisibility` calls had unconnected targets. They changed the entire card instead of its highlight, lock icon, and unavailable overlay. A runtime fixture reproduced an unavailable card becoming hidden. The unavailable indicator also used the availability condition in the wrong direction.

The graph now targets `Border_Highlight`, `LockedIcon`, and `Overlay_Unavailable` explicitly. Decorative indicators use `HitTestInvisible` when shown. An unavailable card stays present but disabled, and its overlay becomes visible. State updates preserve the parent card's visibility.

The Blueprint compiled. A synthetic local-widget check passed all eight combinations of selected, locked, and available, with both visible and collapsed parents (16 cases). It checks each child's visibility, interaction availability, and unchanged parent visibility. Evidence: `Saved/verify_hero_card_visual_state.py`, `Saved/Logs/codex-hero-card-state-20260907.json`, and the `CARD_BEFORE` / `CARD_VERIFIED` records in `Saved/Logs/codex-selection-focus-20260907.log`.

That initial diagnostic log also contains tooling errors and PIE cleanup ensures caused by retained Python references. The reference chains identify `FPyReferenceCollector` retaining the diagnostic world, manager, and widgets. Those references were cleared, unsaved-package checks were empty, and the editor exited normally before a cold verification run. The fixture uses function-local references to prevent recurrence (BF-050).

Cold verification passed all 16 cases again at September 8, 03:23:10 UTC (September 7 local time). `Saved/Logs/codex-hero-card-cold-20260907.log` records the saved asset being loaded in a new editor process and the fixture result. PIE stopped at 03:23:16 UTC. The cold run contains no Blueprint runtime errors, Accessed None, ensure failures, fatal errors, or retained-world cleanup messages through PIE teardown. The editor remains open with PIE stopped.

This repair does not establish that physical keyboard acceptance is fixed. Gamepad input, visible styling, focus retention through live roster refreshes, and packaged behavior remain open. Candidate `LAN-20260907-01` predates this Blueprint change.

## Results identity and portrait integration (September 7, 2026)

Results columns previously marked every teammate as the local player and identified the MVP by display name. Duplicate names could highlight multiple players. An all-negative score set could produce no MVP. Inspection also found no portrait reference in the column data, no full-name tooltips, and no name-color reset when an MVP column was reused for another player.

The first two-peer fixture exposed a further identity defect: all bots had engine player ID 0. Host and client selected different tied bots because their PlayerArray order differed. `UBwayBotCreationComponent` now allocates bot IDs through the authoritative GameSession allocator also used by human registration. Results identify the local player by PlayerState identity, mark exactly the chosen MVP entry, break equal scores by player ID, and use player ID to order duplicate names consistently.

Columns now carry the selected hero asset's portrait. The existing column Blueprint binds it without changing image size, exposes full player/hero names through tooltips, and resets the name color for non-MVP data. Reconfiguration with empty data clears a previous portrait. No new replicated texture property or widget RPC was added; this presentation data is built locally from replicated PlayerStates and local hero assets.

Both editor builds passed: `codex-results-identity-build-20260907.log` (63.52 seconds) and `codex-results-bot-identity-build-20260907.log` (37.28 seconds), under `Saved/Logs`. The column Blueprint and its parent compiled before the second editor restart; only the column asset required changes.

The final same-process PIE fixture had two human peers and six bots. A guarded, development-only authority command supplied duplicate display names, four deaths per player, and selected heroes. After property replication converged:

- All eight player IDs were unique on both peers. Host ID 256 and remote-client ID 264 each had exactly one local-player column.
- With every MVP score at -2, both peers selected ID 256 as the sole MVP.
- After the fixture gave ID 264 ten kills, both peers selected ID 264 alone at score 18. Team-side remapping preserved MVP and local-player identity.
- Both peers had eight portraits. The distinct-winner check compared each portrait directly with that PlayerState's selected hero asset.
- A synthetic column was configured as MVP, normal player with another portrait, empty data, and MVP again. Portrait replacement/clearing, full-name tooltips, and gold/white name-color transitions all passed.

Evidence: `Saved/Logs/codex-results-identity-verified-20260907.log`, `codex-results-negative-tie-20260907.json`, `codex-results-unique-winner-20260907.json`, and `codex-results-column-20260907.json`. Local fixtures: `Saved/verify_results_identity.py` and `Saved/verify_results_column.py`. The final log contains no Blueprint runtime errors, Accessed None, ensure/fatal failures, or retained-world cleanup errors through PIE teardown. PIE stopped and PlayNumberOfClients was restored to its original value of 1.

The earlier `codex-results-identity-runtime-20260907.log` is diagnostic evidence, not a passing run. Its bot-ID check failed and Unreal rejected its generic `set` commands in editor PIE. The replacement native fixture calls the real setters and verifies their results. Do not reuse the old setup commands.

These are synthetic stats and local widget-state checks, not natural-match, physical-input, visual-layout, or packaged acceptance. Final-stat arrival order at the PostMatch transition, gamepad access to full names, bot identity across the full travel/rematch sequence, and clean-device LAN remain open. Candidate `LAN-20260907-01` predates these fixes.

## Authoritative final-results snapshots (September 7, 2026)

The results RPC previously carried only the winner, scores, and round count. Each client built a permanent results cache from its independently replicated PlayerStates. A receive-handler timing probe reproduced ten live kills with zero cached results kills, even after replication converged. That probe used local Python invocation and establishes the stale-cache defect, not RPC transport. Evidence: `Saved/Logs/codex-results-ordering-repro-20260907.json`.

The server now captures the complete summary once and sends it through the existing reliable owning-controller RPC. Each recipient gets its authoritative player identity and team perspective. Widgets consume that snapshot, including scores, totals, MVP, names, and hero IDs. Portrait textures remain local presentation data: the struct excludes them from network serialization and the receiving widget resolves them from the captured hero ID. Both child views use the captured viewer team when arranging columns.

| Object | Authority and delivery |
|---|---|
| PlayerState and scoring components | Server owns live stats and scores; existing gameplay replication continues. |
| GameState | Captures the final summary before iterating recipients; base presentation entry rejects non-authority calls. |
| PlayerController | Reliable Client RPC delivers the complete summary to its owning connection at the results transition. No client-submitted result data is trusted. |
| Results widgets | Local snapshot consumers; no gameplay prediction or reconciliation is involved. |

The change adds no per-tick RPC, relevancy policy, or dormancy tuning. Late arrivals during PostMatch still need separate acceptance; this turn does not establish a replay path for them. Bandwidth was not profiled and no performance savings are claimed.

Validation used one editor build, which passed in 104.29 seconds (`Saved/Logs/codex-results-snapshot-build-20260907.log`). All four results Blueprints compiled after restart without content changes. The editor initially opened its default DevMap; that setup session was stopped and the fixture ran in `L_BW_HeroSelect_Staging`.

The passing fixture had four human PIE peers and four bots, with eight unique IDs and duplicate display names. Every net driver accepted `PktLag=150` and `PktLoss=5`. After the negative-tie baseline converged, `bway.Test.ResultsSnapshot` executed the production results call on a normal server timer tick, outside the Python execution guard. It captured ten kills for ID 277, then reset that player's live stats before the tick ended. The log confirms `authority=1 scriptguard=0`, four sends, and four receiving results screens.

`Scripts/Test/Verify-ResultsSnapshot.py` passed across all four peers. It checked ten captured kills versus zero live kills; every stat field and team aggregate; exactly one matching MVP; exactly one correct local-player column; all eight portrait bindings; and local-team-left mapping in the actual breakdown widgets. Host ID 267 and client ID 276 viewed team 0; clients 277 and 275 viewed team 1. All peers retained the same canonical results.

Evidence: `Saved/Logs/codex-results-snapshot-runtime-20260907.log` and `Saved/Logs/codex-results-snapshot-4-peers.json`. The runtime log has no Blueprint runtime errors, Accessed None, ensure failures, fatal errors, or retained-world cleanup failures through teardown. It contains separate PixelStreaming2 startup messages about GEngine validity; these are outside the results test. PIE stopped, PlayNumberOfClients returned to 1, and dirty map/content checks were empty. Editor PID 56652 remains open.

This verifies the snapshot transport and results data under forced divergence and configured network emulation. The fixture displays synthetic 0–0 results during selection; it does not end a natural match. Natural final-goal capture, separate processes, physical input, visible layout, PostMatch late arrival, rematch/travel, and packaged LAN remain required. Candidate `LAN-20260907-01` predates the fix.

## Natural match, navigation, and scorer credit (September 7, 2026)

A four-peer DevMap run exposed missing static navigation data. All four authoritative bots had running brains and valid target locations, but every spawn and goal projection failed. The client displayed the navigation-rebuild warning. `RecastNavMesh_1` was Static with radius 35 and height 144; one bounds volume existed. Rebuilding navigation took 0.02 seconds. Projection then passed at both spawn sides, midfield, and both goals. The map was saved, unloaded, reloaded, and later loaded by a new editor process; projection still passed. Only `L_BW_DevMap.umap` changed.

The next match completed three natural goals, but all personal Relic Scores remained zero. The results verifier rejected that run, and the visible breakdown confirmed the missing credit. `ARelicActor::OnEnteredGoal` clears CurrentCarrier before `UBwayRoundManagementComponent::OnRelicScored` previously tried to read it. Thrown and passed relics had already lost that reference before entering a goal. The earlier runtime log, `Saved/Logs/codex-results-snapshot-runtime-20260907.log`, preserves both failures and the navigation rebuild; its natural-match sections are diagnostic evidence.

Goal overlap now captures a weak scorer PlayerState together with the scoring team before the delay and carrier detachment. The relic tracks its last possessor on authority through throw, pass, and drop. Round management requires authority, an active round, current roster membership, and matching team before awarding personal credit. It credits that player before EndRound can capture results. Reset clears player and team attribution, and the reset drop cannot restore the old team.

One editor build passed in 106.09 seconds: `Saved/Logs/codex-relic-scorer-build-20260907.log`. A fresh editor then loaded the saved navigation and ran four human PIE peers plus four bots without forced scoring or synthetic stats. Team 0 won 3–0 in three rounds. ID 258 earned two goals and ID 260 earned the winning goal. The server log recorded each personal award before the team score advanced. All four results orchestrators and breakdowns matched every authoritative stat field, scores, round count, team aggregate, MVP, local identity, and hero portrait. The visible remote breakdown displayed the two/one goal split and one highlighted MVP.

A physical click on Client 1's Play Again button initiated server-owned travel. All four peers reconnected with eight unique IDs, round zero, and every match-stat field reset to zero. That full rematch also completed naturally, 3–0 over three rounds. IDs 269 and 271 received the two/one goal split. The four-peer verifier passed again, explicitly requiring all three relic credits. No upgrades were purchased in these runs, so purchased-upgrade reset remains unverified.

After the rematch results were recorded, a separate authority probe exercised pickup followed by drop, throw, and pass. Each release cleared CurrentCarrier while preserving the scorer; resetting cleared player and team attribution. Calling the scoring entry during PostMatch awarded no personal credit. This used local server-side calls and does not establish client release transport or projectile collision scoring. Its first attempt used an unavailable Python `get_pawn` alias; the corrected helper uses GameplayStatics and passed.

Evidence under `Saved/Logs`:

- `codex-relic-scorer-runtime-20260907.log`: natural goals, personal credit, results delivery, physical rematch travel, and teardown.
- `codex-results-natural-scorer-fixed-20260907.json`: first completed match, all four peers.
- `codex-natural-rematch-start-20260907.json`: eight unique IDs and clean stats after reconnection.
- `codex-results-natural-rematch-complete-20260907.json`: completed full rematch, all four peers and three required goal credits.
- `codex-relic-scorer-identity.json`: release/reset identity and PostMatch rejection probe.

PIE stopped and the original client count of 1 was restored. Dirty map/content checks were empty. The new runtime log has no Blueprint runtime errors, Accessed None, ensure/fatal failures, or retained-world cleanup failures through teardown. Existing PixelStreaming2 startup errors are separate from these tests. Current editor PID: 33040.

This closes the recorded same-process natural-results and full-rematch gaps. Separate-process and packaged LAN, purchased upgrades, projectile goal collision, disconnect timing, physical keyboard/gamepad coverage, and clean-device testing remain open. Visible character mesh/animation integration and debug text still need polish; four local render views also showed GPU-memory pressure during the earlier run. Candidate `LAN-20260907-02` is being prepared to carry these fixes into packaged verification.

## Packaged frontend discovery and late-join hero admission (September 7 local / September 8 UTC)

Candidate 02 build/cook/stage passed in 170.26 seconds, exit 0. Its executable SHA-256 matched the staged manifest. The current project patch matched its recorded patch after newline normalization; subsequent committed changes were workflow documentation/helpers only. The untracked chunk-layer input hash also matched. Evidence prefix: `Saved/Logs/codex-packaged-LAN-20260907-02`.

Physical frontend input on host PID 69364 followed Play Lyra → Start a Game → Network LAN → Local (Listen Server + Bots). The host reached staging and then Dorado after the normal 60-second selection timeout. A separately launched client, PID 37900, used Play Lyra → Browse. Its browser listed Local / Listen Server + Bots at 1/8 players; clicking the row joined the active Dorado match. No direct-IP command or host/join fixture was used. This proves two-process discovery and join on one PC, not four-human pre-match assembly or second-PC LAN.

The join reproduced an incomplete gameplay admission path. The client spawned with 100 health and six empty ability slots. The server logged `ApplyHeroDataToNewPawn: ... has no hero selected`, followed by the SkipHeroSelection bypass. Staging's travel option correctly skips the shared phase for carried players, but a newly connected player has no carried hero. The live match completed naturally and the remote client displayed the breakdown, including credited personal goals. No full stat verifier ran on these packaged peers.

The remote Return to Lobby button returned both peers to the frontend. The client logged an ensure in `LyraFrontendStateComponent.cpp:88` because frontend initialization assumes `GetAuthGameMode()` is available. This is an open defect, so frontend return is not a clean acceptance pass. Both test games were closed with Alt+F4 and logged normal engine shutdown. Logs: `codex-packaged02-frontend-visible-20260907.log` and `codex-packaged02-lan-client1-20260907.log` under `Saved/Logs`.

Other observed gaps: Lyra sample/test playlists and mock menu actions remain visible; placeholder meshes include large/offset characters; relic request widgets warn about a dynamic material used as another dynamic material's parent. These are outstanding polish/correctness work, not final-art exclusions.

### Active gate: late join receives a playable selected hero

- Expected result: an arrival during Warmup, Playing, or PostRound sees individual hero selection without a gameplay pawn. Existing peers continue. A valid server-accepted lock spawns the chosen kit and closes selection. Invalid hero IDs and repeated locks cannot bypass admission.
- Source change: GameMode establishes team, URL-selected identity, and the individual gate before `Super::PostLogin` can start a pawn. A weak controller set blocks all restart paths until the selection manager reports an accepted lock. Completion runs next tick, after the manager's array update, and disconnect removes the pending controller. The global selection phase is not restarted. ForceHumanoid remains an explicit development path.
- Client contract: existing PlayerState selection/lock RPCs carry intent. The server validates a loadable hero and existing team-availability rules. The owning controller receives the existing show/hide UI RPCs. Selected hero/lock remain replicated PlayerState data; no per-tick RPC or new replication system was added. The widget waits for server completion while it has no gameplay pawn and hides the expired shared selection timer.
- Build: one LyraEditor compile passed, exit 0, in 86.66 seconds (`codex-latejoin-hero-build-20260907.log`). Editor PID 37064 restarted with the new binary; `WBP_BW_HeroSelect_New` compiled successfully.
- PIE attempts: the first diagnostic's explicit `NumBots=7` was consumed by both installed bot spawners and created 14 bots, with AI-controller Accessed None errors. The second attempt removed that override, produced eight players, and ran through round resets. The extended match used PointsToWin=99 and RoundDuration=300; this is diagnostic evidence only. The editor's Add Client control was not successfully exposed, so no PIE late-join pass is claimed. Runtime log: `codex-latejoin-hero-editor-20260907.log`.
- Cleanup: PIE stopped; client count restored to its observed value 4, extra options emptied, experimental late join restored false. Dirty map/content lists were empty. The bridge stop action reported a forced editor termination after its graceful-close timeout; do not record it as a graceful editor exit. Prefer an explicit editor quit and process observation on the next shutdown.
- Package: candidate 03 build/cook/stage passed, exit 0, in 198.10 seconds from HEAD `3dc6a555` plus the recorded source patch. One packaging attempt; full evidence prefix `Saved/Logs/codex-packaged-LAN-20260907-03`. Executable SHA-256 matched the manifest: `5BE0F9ED36919591E9512153D1AC4B4242335964FCB68161D33DADDB0F0CB453`.
- Packaged verification: host PID 34708 created a LAN session through Play Lyra → Start a Game → LAN → Local. Client PID 76840 discovered the 1/8 listing through Browse and joined after gameplay began. At 05:43:54 UTC the host logged `LateJoinHeroSelection: awaiting choice for player=265 pawn=None phase=EBwayMatchPhase::Playing`. The client displayed individual selection while two round transitions continued. Physical Argus selection and Lock Selection produced the accepted server lock and `completed player=265 hero=HeroDataAsset:DA_BW_HeroData_Argus pawn=B_BW_Character_Humanoid_C_2147477965` at 05:44:52 UTC. Selection closed; the client showed 500 health and six populated slots. Physical F input started the mobility cooldown. This passes the observed valid-choice admission path on two separate processes on one PC. Invalid-ID, repeated-lock, pending-selection disconnect, and PostMatch-arrival probes remain open.
- Match continuation: the client retained its kit through a round reset and reached natural final results. The visible breakdown showed its Argus identity and five personal goal credits split two on the losing team and three on the winning team (two plus one), with one highlighted MVP. No full packaged stat snapshot verifier ran. Physical remote Return to Lobby returned both peers to the frontend but reproduced the client `ensure(GameMode)` at `LyraFrontendStateComponent.cpp:88`; this remains a failed clean-return gate.
- Newly exposed selection UI gaps: the unlimited individual timer renders `Lock in: -1s`; a PostRound summary can cover the still-active selection screen. The placeholder Argus mesh also reports a null AnimationBP and appears oversized/offset; that integration gap remains in scope.
- Runtime evidence: `Saved/Logs/codex-packaged03-lan-host-20260907.log` and `Saved/Logs/codex-packaged03-lan-client1-20260907.log`. The user cleared the candidate's firewall prompt after Computer Use found no targetable PickerHost window. No additional security settings were changed by the agent.
- Next action: fix the reproduced selection timer/overlay defects and client frontend initialization, then compile and verify affected paths. Preserve candidate 03 as admission evidence. Invalidate admission evidence after changes to login order, selection validation/locking, restart gates, or UI completion. Four-human and second-PC acceptance remain open.

### Selection presentation and frontend initialization follow-up

The accepted late-join fix and candidate-03 evidence are committed as `a24c3ec5`. The follow-up keeps the existing UMG/CommonUI ownership: replicated gameplay state drives the owning client's selection screen; PlayerState RPCs still submit hero intent. Native widget code caches the authored `Widget_Timer` child without changing its Blueprint type and collapses it when the shared countdown is inactive. PlayerController dismisses an existing PostRound summary when selection opens, suppresses further summaries while selection is active, and restores a still-active summary when selection closes. Frontend initialization reads the hard-disconnect option from GameMode when available and the world URL for connected clients, preserving user/session cleanup without asserting on normal client ownership.

The first editor build passed in 49.21 seconds, but an overly broad native `BindWidget` declaration changed the Blueprint timer reference type and broke two text-update graph connections. The bridge's compile action incorrectly reported success; starting PIE exposed the compiler dialog. The corrected implementation uses a separately named transient cache and preserves the existing Blueprint variable. The second build passed in 33.25 seconds, and the dedicated Blueprint validator reported zero errors and warnings after cold restart. Logs: `codex-selection-frontend-build-20260907.log`, `codex-selection-frontend-build2-20260907.log`, and `codex-selection-frontend-verify2-20260907.log` in `Saved/Logs`.

Canceling that first PIE compilation dialog exposed a bridge crash at `GameThreadExecutor.cpp:56`: a request timed out while the game thread was blocked, then the delayed ticker dereferenced expired stack state. A pre-existing worktree change had removed the lifetime protection already committed in `d32a143d`. The file was restored to the committed implementation; no unrelated bridge edits were changed. Its existing `ProjectB.MCP.QueuedTimeoutLifetime` automation test passed after the second build. The failed attempt is retained in `codex-selection-frontend-verify-20260907.log`. Dirty map/content lists were empty before the failed PIE attempt. Editor PID 65036 crashed; do not record that exit as graceful.

Four-peer PIE verification after the second build reached a natural one-goal result and used a physical remote Return to Lobby click. Every peer reached the frontend, with no GameMode ensure or fatal failure. However, PIE sent the remote peers through `?closed` into standalone frontend worlds, where each had a GameMode; this verifies that cleanup route, not the connected packaged-client branch. `codex-selection-frontend-return.json` records that distinction. Packaging and connected return/rehost remain required.

In a fresh four-peer PIE run, all four timed selection screens had positive remaining time and a visible countdown. Reopening local selection presentation on the three already-connected clients after the shared phase ended produced remaining time -1 with the timer collapsed on all three. These are presentation fixtures, not new network arrivals. Evidence: `codex-selection-frontend-timed.json` and `codex-selection-frontend-untimed.json`. The untimed probe ran during PostMatch, so it does not close the pending PostMatch-arrival gate. Several initial Python inspection attempts used unavailable aliases or protected properties; the corrected helper discovers actual widgets through WidgetBlueprintLibrary and retains no world wrappers across callbacks. The attempted instance time-limit edit was rejected without mutation.

A third four-peer PIE run attempted to open local selection once every peer was Playing, but that round ended between the observer's 0.25-second samples. The observer was stopped and a fourth run moved setup into Warmup. This passed: a natural PostRound summary appeared on the control host while all three remote selection screens remained active with collapsed timers and no overlapping summary. Closing selection on one remote peer restored its still-active round summary. Evidence: `codex-selection-frontend-postround-overlap.json`, `codex-selection-frontend-postround-restored.json`, and `SELECTION_OVERLAP_PROBE_PASS` at 06:18:27 UTC in the runtime log. These are local presentation probes on existing peers; candidate 03 separately proves actual network admission.

PIE stopped; client count restored to four and extra options cleared both in memory and in the saved editor settings. Dirty map/content lists were empty. The observer unregistered itself after passing. Editor PID 30888 exited normally after explicit `QUIT_EDITOR`, with `LogExit: Exiting` at 06:21:22 UTC. The proven helper is retained as `Scripts/Test/Verify-SelectionPresentation.py`; its usage and limits are in the test README. Candidate 04's package script is prepared. Follow-up counts: two editor builds, one canceled pre-PIE attempt, four running PIE sessions, one existing bridge regression test, no package yet. Next: build candidate 04, repeat connected packaged-client frontend return without a GameMode, and rehost/discover/join. Four-human and second-PC LAN remain required.

Candidate 04 subsequently completed build/cook/stage, exit 0, in 149.62 seconds (one package attempt). The source changes and helper are committed as `8816d0f6`; packaging started from base `a24c3ec5` while the follow-up files were staged. The initial plain `git diff` omitted those staged edits, so supplementary `-project-staged.patch` and `-project-complete.patch` files were captured during the build before any further runtime-source change. `-staged-source-supplement.json` records the correction and runtime source hashes. These supplement the base/status/engine/untracked-input records under `Saved/Logs/codex-packaged-LAN-20260907-04`. Future source snapshots must use `git diff HEAD --binary` to include staged changes.

The candidate executable SHA-256 matches its manifest: `313B57B9CF7B980ACBCE9FCBBF787E8B89DF2A0A4B28046377E0D0C2ECFEBAC0`. Host PID 57772 launched at the frontend with log `Saved/Logs/codex-packaged04-lan-host-20260907.log`. Connected-client return/rehost verification is in progress; no candidate-04 LAN acceptance pass is claimed yet.

Candidate 04's first two-process test passed frontend LAN discovery, late-join selection, natural completion, and connected Return to Lobby on one PC. Host 57772 created Local / Listen Server + Bots through the frontend. Client 25632 discovered its 1/8 listing at 23 ms, joined during Playing, and remained without a pawn at 06:32:47 UTC. The host staging countdown was visible; the client's individual countdown was absent. Selection stayed clear through a PostRound transition. Physical Rawlins selection/lock at 06:33:42 produced the accepted Gunslinger hero ID and spawned pawn, 400 health, and six populated slots. The match finished 3–0; the visible breakdown showed three personal goal credits split two plus one and one highlighted MVP.

Physical remote Return to Lobby at 06:35:40 returned the host to `L_LyraFrontEnd?listen` and reconnected the client to the host's frontend (`Welcomed by server` and the host-address frontend URL), with no ensure, fatal error, or Accessed None in either runtime log through return. This covers the connected-client branch that PIE did not exercise. Client 25632 then exited normally with Alt+F4 at 06:36:40. Its log is `Saved/Logs/codex-packaged04-lan-client1-20260907.log`. Rehosting keeps host PID 57772 alive and will use a fresh client to verify discovery of a newly created session. Full four-human, same-party rehost, second-PC LAN, and full packaged stat comparisons remain open.

### Candidate 04 rehost and PostMatch arrival outcome

- Expected result: the existing host returns to the frontend, creates another LAN session, and admits a fresh client into a playable flow. The connected return and discovery portions pass; complete admission/rematch acceptance fails below.
- Same-process rehost passed: host PID 57772 logged successful session creation at 06:38:15 UTC and loaded staging at 06:38:19. This second match finished naturally before the fresh client joined. The host executable was not restarted.
- One discarded client launch (PID 57072, `codex-packaged04-lan-client2-20260907.log`) reached the frontend in the sandbox but exposed no targetable Computer Use window despite `-WindowStyle Normal`. Window and app inventory confirmed only the host. This is no gameplay evidence. A graceful process-close request returned true but did not end the process. The retry changed execution context: the verified executable was launched on the interactive desktop through an approved escalation, PID 69384, with a new log `codex-packaged04-lan-client3-20260907.log`.
- Fresh discovery/admission transport passed: physical Play Lyra → Browse found the newly created Local / Listen Server + Bots listing at 1/8 and 14 ms. The client clicked it and was welcomed into Dorado at 06:45:59 UTC.
- PostMatch arrival failed: the client displayed the finished 3–0 score, a default humanoid with 100 health, six empty ability slots, and a waiting-for-additional-players banner. It received neither hero selection nor the completed match breakdown. This is an actual separate-process arrival after match completion, unlike the earlier local presentation fixture.
- Physical host Play Again at 06:46:35 initiated nonseamless rematch travel. Its requested URL contained `?-SeamlessTravel?-SkipHeroSelection?-Restart?NoSeamlessTravel?listen`, but the resulting LoadMap URL at 06:46:39 still contained `SkipHeroSelection=1` and `SeamlessTravel`. Option augmentation subsequently preserved them. The host logged `ApplyHeroDataToNewPawn: ... has no hero selected` for both human controllers at 06:46:40 and again after round reset. The remote client remained at 100 health with six empty slots while the next match scored naturally. This fails the playable packaged rematch gate and is broader than the new client's PostMatch arrival.
- Interpretation to verify in source: option removal is ineffective for the inherited staging-derived travel URL, while nonseamless travel resets human hero state. The prior direct-map PIE rematch did not begin with that staging skip option and does not cover this path. PostMatch login also requires finished-match state delivery and an appropriate spawn gate.
- Neither host nor interactive client log contained an ensure, fatal error, Accessed None, or `Error:` through this reproduction. Existing material and placeholder mesh/animation warnings remain visible. Absence of these error signatures does not make the failed gameplay gates pass.
- Counts: one candidate-04 package (149.62 seconds), no further builds; one original host, one first connected client, one discarded sandbox client, and one interactive fresh-client retry. The same host created two sessions and then initiated one rematch. Earlier source/PIE attempt counts remain above.
- Next gate: correct rematch URL/state handling and PostMatch arrival. Require valid human selection and populated kits after a staging-derived rematch, plus a coherent finished-match screen for a new PostMatch arrival. Inspect the relevant source, run the cheapest affected checks, then package once. Invalidate these results after changes to session cleanup, travel options, login ordering, selection admission, or results catch-up.
- Cleanup: physical Alt+F4 closed interactive client 69384 normally (`LogExit: Exiting` at 06:49:47 UTC) and host 57772 normally (06:50:32). The unused sandbox client 57072 ignored its graceful-close request and was explicitly terminated after its executable path was verified; do not call that a graceful exit. All three processes were absent afterward. No editor or build was launched during this final gate. Runtime error-signature screening remained clean through the interactive processes' teardown. The failed rematch was stopped without claiming full-match acceptance.

### Rematch travel base and retained PostMatch results (September 8)

The source fix clears exact transition-option keys from `FWorldContext.LastURL` before relative rematch travel. This base is used both by GameMode's first FURL parse and the later Browse parse; consuming removal tokens only in the first parse allowed the second to restore them. Persistent rules, unrelated option prefixes, and the actual listen port are preserved. Rejected travel restores the entire previous base. No engine source change is required.

GameState now retains its first authoritative final summary until world replacement. Existing participants and new PostMatch connections receive copies through the existing owning-controller results RPC. Participant identity and team come from the captured columns, so later roster changes do not rewrite the completed match. A visitor receives no invented player column or win/loss interstitial. Authority blocks gameplay restarts during PostMatch, and results presentation closes any still-active individual selection screen.

| Owner | Contract and transport |
|---|---|
| Server GameMode / GameState | Login and rematch decisions remain authoritative. Play Again is existing controller intent; server requires idle PostMatch. GameState retains completed totals and sends a viewer-specific copy at completion or PostMatch login. |
| Owning PlayerController | Existing reliable client RPC carries the complete result snapshot, independently of PlayerState replication order. Widgets remain local. Existing replicated match-phase and hero state continue to drive gameplay; no prediction, relevance, dormancy, or update-rate changes were introduced. |

Verification so far:

- First editor compile failed in 52.23 seconds because the new test included nonexistent `Engine/URL.h`. The declaration is in `Engine/EngineBaseTypes.h`; after correcting that include, the second compile passed in 5.88 seconds. Logs: `codex-rematch-arrival-build-20260908.log` and `codex-rematch-arrival-build2-20260908.log`.
- Cold editor PID 46692 loaded the compiled module. `Breakaway.FrontEnd.RematchTravelBase` passed at 07:01:34 UTC. It first reproduces the old double-parse restoration, then checks the fix, persistent rules, unrelated prefixes, listen flag, and PIE port. `BP_BW_GameState` validation returned zero errors and warnings.
- One four-peer listen-server PIE run used the direct DevMap with PointsToWin=1, WarmupDuration=5, PostRoundDuration=3, and RoundDuration=90. Natural scoring finished 1–0. The existing verifier compared all results fields, identities, portraits, and aggregates on all four peers with authority. `codex-results-rematch-arrival-source.json` records the pass; `codex-rematch-arrival-postmatch-source.json` records PostMatch on all peers and the authority spawn-block predicate. This is not a new connection or staging-derived packaged rematch.
- PIE stopped with empty dirty map/content lists. Editor exited normally at 07:04:34 UTC. Four-client defaults and empty extra server options were restored on disk after exit. Runtime log: `codex-rematch-arrival-editor-20260908.log`; the known PixelStreaming2 startup error is separate from gameplay. No ensure, fatal error, or Accessed None was observed.
- Counts: two editor builds, one URL regression test, one Blueprint validation, one running four-peer PIE session. The next candidate script is `Saved/package-LAN-20260908-01.ps1`; both project and engine snapshots now use `git diff HEAD --binary`. It has not yet run.

Next packaged gate: create a LAN session through staging, finish naturally, admit a fresh PostMatch client, and require the unchanged breakdown without a default gameplay pawn. Then use Play Again, require human selection and populated kits, and verify another natural match. Four-human frontend assembly, pending-selection disconnect, invalid/repeated lock, lag/loss on these paths, same-party rehost, second-PC LAN, and performance traces remain separate open requirements.

Candidate `LAN-20260908-01` subsequently built/cooked/staged successfully from commit `d1788afc` plus recorded project/engine patches and the recorded untracked chunk-layer input. One package attempt, 192.94 seconds, exit 0. Staging's first Zen connection was refused; its built-in retry restarted the local service and completed without agent intervention or another package attempt. Evidence prefix: `Saved/Logs/codex-packaged-LAN-20260908-01`. Executable SHA-256 matches the manifest: `FFE3F1F089E4D35150F57DA0182F7E08877196A6A2822A857AA78E94F363B99B`.

Interactive host PID 30292 and fresh client PID 13272 use logs `codex-packaged0801-lan-host-20260908.log` and `codex-packaged0801-lan-client1-20260908.log`. Both launched on the interactive desktop. The host created LAN / Local through the frontend and physically selected/locked Argus in staging; it reached Dorado with 500 health and six populated slots. The client remains outside the match until PostMatch for the targeted arrival check. Initial client mouse actions focused menu buttons; Return activated Play and Browse. No new security dialog was exposed during these launches. Full packaged gate verification is in progress.

Candidate 0801 runtime outcome:

- Host completed a natural 1–3 match at 07:17:41 UTC. The visible breakdown contained eight players, four personal goal credits (one on the losing team; two plus one on the winning team), and one MVP.
- The fresh client discovered the 1/8 LAN listing at 32 ms and joined after completion. At 07:18:51 authority logged `PostMatchArrival: player=265 pawn=None resultsSent=1` and retained results with `participant=0`. The client displayed the same eight columns, totals, portraits, and MVP as the host, with no invented visitor column. Its results log reports 1–3 across four rounds. This passes actual separate-process PostMatch snapshot delivery and the observed login spawn gate on one PC; no full packaged field-by-field verifier ran.
- The gameplay HUD appeared behind that breakdown. The log confirms the results screen initially suppressed its gameplay layer before the experience later pushed the HUD. CommonUI's displayed-widget transition then restored container visibility. This is a separate presentation failure.
- Physical Play Again on the arriving client initiated rematch at 07:19:38. At 07:19:42 the server loaded Dorado with `Experience`, `listen`, and `NoSeamlessTravel`; neither selection-skip nor seamless flags returned. URL cleanup passed. However, humans remained without playable pawns or populated kits while match phases advanced. Source inspection confirmed that direct-arena auto-selection startup is editor-only; the packaged rematch must enter normal staging. This remains a failed playable-rematch gate.
- Interactive client exited normally at 07:24:01 and host at 07:25:02; both were absent afterward. No ensure, fatal error, Accessed None, or `Error:` appeared in these runtime logs. The failed rematch was stopped without claiming completion.

Follow-up source routes rematches through `HeroSelectLevel`, a configurable soft map defaulting to the existing shared staging map. It carries the current arena and loaded experience as the selection destination. The fixed relative-travel base and port handling remain in place. Results now subscribe to the gameplay container's displayed-widget change event, reapply suppression after CommonUI changes visibility, and remove the delegate/restore the intended visibility during destruction. There is no polling or permanent global layer change.

One follow-up editor build passed in 55.14 seconds (`codex-rematch-staging-hud-build-20260908.log`). Cold editor 56980 validated `BP_BW_GameState` and `WBP_BW_ResultsWidget` with zero errors/warnings and confirmed the Blueprint's new map default resolves to the existing staging world. Dirty map/content lists were empty; the editor was explicitly quit. Log: `codex-rematch-staging-hud-editor-20260908.log`. These are compile/default checks; the new route and layer lifecycle still require runtime verification. Candidate script `Saved/package-LAN-20260908-02.ps1` is prepared for that check.

The follow-up was committed as `fd99a9ce`. Candidate `LAN-20260908-02` subsequently completed build/cook/stage in 167.37 seconds, exit 0, on one attempt. Staging recovered from its first refused Zen connection through the built-in retry. Source/patch/status, untracked input, build log, exit code, and manifest use prefix `Saved/Logs/codex-packaged-LAN-20260908-02`. Executable SHA-256 matched its manifest: `AD521761655C1175ECEBE3F20D7C350AA9F8515162F0C420953FDBDE6F111672`. Editor 56980 exited normally at 07:30:13 UTC. No editor, test game, or build remained after packaging. Candidate 0802 has not yet run; use it directly for PostMatch arrival, late-HUD suppression, staging selection, selected kits, and natural rematch completion.

### Candidate 0802 PostMatch admission and staging rematch

- Expected result: an actual PostMatch visitor receives the retained final breakdown without a gameplay pawn or gameplay HUD; remote Play Again lets both humans select and lock heroes in shared staging, travel to the same arena/experience with populated kits, and complete a natural rematch.
- Reused candidate 0802 with its recorded executable hash and no intervening runtime-source changes. One host process (27876) and one client process (34076), on one PC, were launched visibly with approved interactive launches. No extra build or package was needed for this test. Logs: `Saved/Logs/codex-packaged0802-lan-host-20260908.log` and `codex-packaged0802-lan-client1-20260908.log`.
- Physical host flow: Play Lyra → Start a Game → LAN → Local / Listen Server + Bots → Argus → Lock Selection. Staging loaded at 07:44:18 UTC; the arena started at 07:44:54. Initial host HUD showed 500 health and six populated slots. Bots completed the match naturally, 1–3 across four rounds, at 07:46:58.
- Client remained outside the session until PostMatch, discovered Local at 1/8 and 40 ms, and joined physically. At 07:47:27 authority logged `PostMatchArrival: player=265 pawn=None resultsSent=1` and `participant=0`. Both screens showed the same eight columns: losing-team gold 197 per player, winning-team gold 397, personal goal credits 1 versus 1+1+1, and one winning Argus MVP. The visitor received no invented column. Its gameplay HUD remained hidden on a second observation after loading settled. Actual PostMatch delivery, observed login spawn gate, and late-HUD suppression pass in this two-process scope. No packaged field-by-field verifier ran.
- Remote Play Again loaded shared staging at 07:48:20, carrying Dorado and `B_BW_Experience_CaptureTheRelic` as its destination. The client physically chose and locked Rawlins. The host screen appeared locked to its previous Argus and could not select Korryn; readiness stayed 1/2 until the 60-second timeout. Logs show two phase-controller starts and two travel requests; a late unlock followed the first UI push. Full manual rematch selection therefore fails despite successful staging travel.
- Timeout fallback traveled both humans into Dorado at 07:49:20. Host Argus had 500 health; remote Rawlins had 400; both had six populated kit slots and the gameplay HUD was restored. The rematch completed naturally 0–3 at 07:51:01. Both breakdowns showed the two human columns, matching gold 79/379, three personal goals split 2+1, and one Rawlins MVP. Playable timeout-fallback rematch and results propagation pass; do not count this as both humans freely selecting/locking.
- Neither runtime log contained `Error:`, ensure, fatal error, or Accessed None through completion. The known waiting-for-additional-players banner still overlaps participant results; placeholder meshes/animation and debug messages remain unfinished polish. Client exited normally at 07:51:37 and host at 07:52:14; both processes were absent afterward.
- Retry condition: correct selection lifecycle so a returning host is editable before UI activation and exactly one phase controller owns startup/completion. Verify in the editor first, then package the changed source. Four-human assembly, same-party rehost, second-PC LAN, and physical all-kit input remain separate gates.

### Follow-up: returning-host selection lifecycle

The phase component now registers current PlayerStates before resetting carried locks. Only the component referenced by the GameState orchestrates startup and skip requests; extra instances do not subscribe to phase/experience callbacks. The selection widget refreshes unlocked presentation on activation and PlayerState replacement. Its Blueprint uses one shared refresh path for lock and unlock: all hero cards and the lock button derive enabled state from `IsSelectionLocked`, and the button chooses the corresponding localized label.

One C++ build passed in 39.98 seconds (`codex-rematch-selection-build-20260908.log`). `BP_BW_GameState` and the final `WBP_BW_HeroSelect` validate with zero errors/warnings. The Blueprint asset was backed up under `Saved/WBP_BW_HeroSelect-before-unlock-20260908.uasset` before editing. No bridge source or engine source was changed.

Verification attempts are retained rather than counted as passes:

- Editor 17172 was used for read-only Blueprint inspection, then exited normally at 07:59:02 UTC before compiling.
- In editor 10892, the first single-peer PIE run completed 1–0 but the new event did not restore controls. The bridge had created its `K2Node_Event` without `bOverrideFunction`; this was corrected explicitly. Blueprint validation also caught the const-reference text input, which was changed to a wired localized literal. The compile tool's success flag alone did not catch that graph error.
- A local presentation probe in the second PIE run failed while constructing a `PrimaryAssetId`, before its intended UI mutation. Its persistent Python globals retained PIE objects. Reference-chain logs identify `FPyReferenceCollector` as the owner keeping the old world alive. Subsequent travel produced GC ensures and an `AsyncGameplayMessageSystem` assertion at 08:13:29. Editor 10892 crashed; do not call that a graceful exit or gameplay acceptance. `codex-rematch-selection-verify-20260908.log` records this test-setup failure. No successful presentation-fixture report was produced.
- Fresh editor 4964 used physical inputs and log reads only during PIE. Its first run exposed the remaining disabled hero-card controls after the lock button was restored; that run stopped without a travel/GC error. The Blueprint was changed to refresh every control from the same lock state.
- The next run physically selected/locked Argus and completed naturally 1–0 at 13:54:26 UTC. Play Again started one shared staging phase at 13:54:55. The returning host physically changed to Korryn and locked before timeout; one arena travel occurred at 13:55:20. Korryn then had 335 health and six populated slots in the same DevMap/Dev experience. This passes manual host reselection and selected-kit travel in one-peer PIE; the full packaged two-human replay and duplicate-component guard still need their packaged check.

The workflow and bug-prevention catalog now require function-local Python runtime wrappers and serialized return values, including exception cleanup, before any travel or PIE stop. The catalog also records native Blueprint event override flags and wired const-reference text inputs. Counts so far: one C++ build, three editor launches, four single-peer PIE runs, zero new package attempts. Candidate 0803's script is prepared; package only after the clean run and editor cleanup finish.

The final clean rematch completed naturally 1–0 at 13:55:42 UTC. Its breakdown showed the human as Korryn, one MVP, and one credited goal. The clean log contains no ensure, fatal error, assertion, or Accessed None through PIE stop and editor exit; the known PixelStreaming2 startup error is separate. Editor 4964 had no dirty map/content packages and exited normally at 13:57:21 after explicit `QUIT_EDITOR`. Four clients and empty extra server options were restored both in memory and in the saved editor settings. This is a one-peer editor acceptance pass, not a packaged multiplayer pass. No successful Python presentation fixture is claimed.

The five runtime/Blueprint files are committed as `c5e11663`. Candidate `LAN-20260908-03` is now building from that base with the same full project/engine patch and untracked-input provenance workflow. One package attempt is running. Next runtime gate: both separate-process humans must manually choose and lock heroes on rematch without waiting for timeout; require one staging start/travel, both populated selected kits, and natural completion. Candidate 0802's PostMatch/HUD pass remains recorded at its own source revision.

Candidate 0803 subsequently completed build/cook/stage on that one attempt, exit 0, in 170.43 seconds. The built-in retry recovered the initial failed Zen attachment request during staging. Its executable SHA-256 matches the manifest: `8B4EBF8FB637A731A6D8A1D81F170DE293C930C29172A76EA374EBC051154BB0`. Evidence prefix: `Saved/Logs/codex-packaged-LAN-20260908-03`; source base `c5e1166328d95416866ff2f5ef97dc04d25d0428`. No editor, test game, or package remains running. Candidate 0803 has not been launched; packaged manual two-human reselection is the next verification, using this artifact directly.

### Candidate 0803 launch and pending firewall handoff

On the next continuation, HEAD was `d1d12a21`, the 37 unrelated dirty entries remained, no runtime/hero-selection asset change followed the candidate base, and the executable hash still matched. One visible host launched as PID 48272 with `Saved/Logs/codex-packaged0803-lan-host-20260908.log`. Windows displayed a new ProjectB firewall prompt. Computer Use returned the game's window but reported running PickerHost with an empty window list, so no permission UI action was attempted through the game window. The user was asked to verify the candidate path through Show more and choose the narrowest offered LAN scope. The prompt remained visible on reobservation; no client was launched and no gameplay acceptance is claimed. Preserve the host/log and resume after the handoff.

While waiting, repeated log searches were consolidated into `Scripts/Test/Get-PackagedFlowBrief.ps1`. Checks against candidate 0802 reproduced four host selection starts/travels, two results on each process, and zero critical lines; the prior failed editor log exercised critical-line detection and bounded excerpts. A first live-file read failed because the default reader did not share write access. The reader now uses read access with `FileShare.ReadWrite` and disposes in `finally`; it successfully read the running candidate 0803 host. These are helper checks, not new gameplay passes. No runtime source, candidate contents, build, or editor changed during this continuation.

### Waiting-banner audit while candidate 0803 awaits firewall clearance

The user's latest firewall reply named candidate `LAN-20260907-03`. Reobservation of the existing 0803 host still showed the Windows Security prompt. The current host remains PID 48272; no duplicate host, client, editor, build, or package was launched. A request identifying the newer candidate was issued. The token-saving workflow is already present in `AGENTS.md`, this ledger, and the compact test helpers; it does not change account usage limits, and no savings percentage has been measured.

The independent read-only audit found a reproducible phase sequence in `Saved/Logs/codex-packaged0802-lan-host-20260908.log`:

- Lines 4195–4199: at 07:51:01.085 UTC, Warmup begins, PostRound ends early, and the native coordinator decides the match has ended.
- Lines 4242–4245: at 07:51:01.091, PostGame begins and ends that Warmup instance. Thus the final transition briefly entered Warmup despite proceeding to results.
- Serialized asset names show `BW_Phase_PostRound` referencing `BW_Phase_Warmup` and `K2_StartPhase`. Warmup references `GE_BW_PregameLobby`, which contains `GameplayCue.ShooterGame.UserMessage.WaitingForPlayers`. The cue references `W_WaitingForPlayers_Message` through `GCNL_Widget_Base`.
- `UBwayRoundManagementComponent::CompletePostRoundPhase` already owns the final-versus-next-round decision. `HandlePostRoundPhaseEnded` invokes that decision. This makes the Blueprint's transition wiring the next focused inspection.

These observations identify a likely trigger, not a proven widget-lifetime defect. Binary strings do not establish connected Blueprint execution or whether effect/cue cleanup ran. Before editing, inspect the PostRound transition and Warmup cleanup graphs; then verify natural final-round results on both peers, absence of the waiting banner after loading settles, and continued warmup/input protection for nonfinal rounds. Preserve the existing 0803 selection-test artifact; no source or asset changed during this audit.

### Phase ownership and cancellation cleanup — f50104ec

Read-only `UnrealEditor-Cmd -run=pythonscript` exported six relevant assets successfully, exit 0. Before/after text exports and compact graph summaries are under `Saved/Logs/waiting-phase-audit-20260908` and `waiting-phase-audit-20260908-after`. The commandlet log is `codex-waiting-phase-audit-20260908.log`; the export script remains at `Saved/inspect-waiting-phase-20260908.py`.

The exports confirmed connected execution: PostRound applied immunity/input blocking, waited a hard-coded delay, removed effects, and started Warmup. Warmup removed its lobby effect only before its normal Playing transition; its OnEndAbility merely printed a message. A final-round PostRound transition could therefore start Warmup immediately before PostMatch cancelled it, leaving the global lobby effect and its separately attached waiting widget alive.

The committed change has three files:

- `BW_Phase_PostRound`: activation applies its effects; OnEndAbility removes them. The Delay and autonomous Warmup start were removed. The native coordinator now owns the configured pause and final-versus-next-round decision.
- `BW_Phase_Warmup`: OnEndAbility removes `GE_BW_PregameLobby`, including cancellation. Normal-path removal remains safe because the global effect registry checks whether the entry exists.
- `BwayRoundManagementComponent.cpp`: zero-duration PostRound completes on the next tick; entry to Playing starts its GAS phase on every round. The latter is required to end the outgoing PostRound ability and release input blocking.

Verification and retained failures:

1. First build passed in 65.45 seconds (`codex-phase-cleanup-build-20260908.log`). Hidden editor 50484 ran two natural matches with explicit `NumBots=7`. The first observer failed because the Python alias `AbilitySystemBlueprintLibrary` was unavailable; reflection through its native class path corrected the probe. Its final manual snapshot still confirmed no waiting widget/lobby effect at PostMatch (`codex-phase-cleanup-two-second-postmatch.json`). The next zero-duration run correctly caught input blocking persisting in round two: native code previously started the Playing GAS phase only on first entry. This failed implementation was corrected before any completion claim.
2. Both initial runs also exposed 15 player states and `B_BW_AI_Controller_BallMode` startup Accessed None errors. Both legacy Lyra and Breakaway spawners read the explicit bot-count override. This separate defect remains unresolved; removing that test override uses normal roster scaling, not a fix for the override. The eight-player expectation was retained. Reports also contain repeated relic-carrier effect entries that need a separate cleanup audit.
3. Second build passed in 25.84 seconds (`codex-phase-cleanup-round2-build-20260908.log`). Fresh hidden editor 69528 then ran the final zero- and two-second cases with full PIE game flow enabled, normal roster scaling, PointsToWin=2, RoundDuration=30, and WarmupDuration=3. No synthetic goals or phase mutations were used. Both completed two natural rounds with eight player states. The observer recorded the lobby effect on all eight during Warmup and no lobby/input-block effects or waiting widgets in active rounds or final results. The two-second case also recorded input blocking on all eight in the nonfinal PostRound, followed by its removal in round two.
4. Final reports: `codex-phase-cleanup-final-zero-second.json` (38.297 seconds) and `codex-phase-cleanup-final-two-second.json` (33.406 seconds). The latter final PostRound snapshot sampled before its GAS application; nonfinal PostRound supplies the positive protection evidence. Runtime log: `codex-phase-cleanup-final-editor-20260908.log`. Both phase Blueprints validated with zero errors and warnings after the cold build. Final runs, PIE stop, and editor shutdown had zero ensure/assertion/fatal/Accessed None/Python/PIE error lines.

All probes retained scalar reports only across callbacks, and callbacks were unregistered before stopping PIE. Original settings were restored: four clients, one process, empty extra server options, and the previous full-flow flag. Dirty maps/content were empty before each explicit editor quit. Editor 69528 exited normally at 14:53:11 UTC. No project editor or build remained; the existing 0803 game host was preserved, and its firewall prompt was still visible on reobservation.

Attempt accounting: one inspection commandlet, two builds, two hidden editor launches, four PIE runs, no new package. `Saved/verify-phase-cleanup-20260908.py` and its settings snapshot preserve this scoped probe. This is one-authority NullRHI state evidence, not rendered UI, physical input, network transport, or packaged acceptance. Candidate 0803 remains based on c5e11663 and does not contain f50104ec. A newer candidate must verify this fix visually on both peers. Next independent work is the explicit bot-count ownership failure while the current firewall handoff remains pending; also inspect the initial legacy Playing-before-Warmup transition observed in the full-flow log. BF-111 records root cause and prevention.

### Explicit bot counts and roster ownership — 70bea97c

`B_BW_Experience_Dev` and `B_BW_Experience_CaptureTheRelic` each injected `B_BW_BotSpawner_BallMode`, a legacy Lyra bot manager, while `BP_BW_GameState` already provided the current Breakaway manager. Both read NumBots, so the explicit value seven activated two spawners and produced 15 player states. The legacy zero default hid the problem in ordinary runs.

Only those two experience assets changed. One legacy component entry was removed from each AddComponents action. A dry run required exactly one matching entry per experience. The editor mutation compared all remaining actor/component classes, client/server flags, addition flags, action order, and action classes before saving. Both Blueprints validated with zero errors/warnings. `Saved/Logs/bot-ownership-20260908/{before,after}.json` records the composition; `Saved/fix-bot-ownership-20260908.py` records the scoped procedure.

Runtime evidence (`Saved/Logs/codex-bot-ownership-editor-20260908.log`, hidden editor PID 74052):

- The first DevMap NumBots=7 observer failed because call_method requires native `K2_GetComponentsByClass`, not its ScriptName `GetComponentsByClass`. That match still completed naturally; its postmatch snapshot had seven possessed modern bots and eight player states. The failed observer report is retained as `codex-bot-ownership-dev-seven.json`; the full sequence was rerun after correcting the probe.
- `codex-bot-ownership-dev-seven-recheck.json`: passed in 27.5 seconds, two natural rounds, seven possessed `BP_BW_RelicBotController_C` bots with unique player states, eight total player states, one native manager, and zero legacy managers.
- `codex-bot-ownership-dev-zero.json`: passed in 9.157 seconds, one player state, zero AI/controllers/bot player states, one native manager, and no legacy manager. This tests startup/active-round roster only; a botless match was not claimed complete.
- `codex-bot-ownership-capture-four-peers.json`: Dorado with the CaptureTheRelic experience, four same-process PIE player instances, explicit four bots, and three natural rounds. Passed in 76.156 seconds. Authority had four possessed modern bots and eight player states. All four worlds reported IDs 273–280; owning IDs were 273, 280, 279, and 278. No forced goals or synthetic roster mutations were used.

The four-peer run exposed 10,415 Blueprint error lines, all in `B_FootStep` against a missing `NS_FootPrint` Niagara component under NullRHI. There were zero legacy bot-controller errors and zero ensure/assertion/fatal lines. This roster check is not a clean visual/FX acceptance result; inspect optional Niagara spawn/lifetime handling next. Probes kept only scalar reports between callbacks and unregistered before PIE stop.

Settings were restored to four clients, one process, empty extra server options, and the original full-flow flag; the editor returned to DevMap. Dirty maps/content were empty before explicit shutdown at 15:20:07 UTC. A fresh read-only Python commandlet then compared the saved compositions to after.json and found exactly two matching experiences with zero legacy requests, exit 0. Evidence: `bot-ownership-20260908/cold-verification.json`, `codex-bot-ownership-cold-20260908.log`, and `Saved/verify-bot-assets-cold-20260908.py`.

Attempt accounting: one hidden editor launch, four PIE runs including the failed observer, one cold commandlet, zero C++ builds, and zero packages. Commit `70bea97c` contains only the two experience assets. BF-112 documents prevention. Rendered, physical-input, separate-process, and packaged LAN acceptance remain open.

After the bot checks, candidate 0803's preserved host PID 48272 was still running. Direct reobservation now showed its frontend without the firewall dialog. Its executable hash still matched `8B4EBF8FB637A731A6D8A1D81F170DE293C930C29172A76EA374EBC051154BB0`, and the planned client log was absent. Resume that candidate for the pending c5e11663 manual reselection check; it does not contain the newer phase or bot fixes.

### Candidate 0803 manual rematch and shared frontend rehost

Expected result: two returning player instances can each change heroes and physically lock before the 60-second staging timeout; each selection starts once and travels to the arena once; both receive the chosen kits and reach natural results. Then the same connected players can return through the frontend and start another match without restarting either process.

Reused the hash-verified 0803 artifact with host PID 48272 and one visible client PID 53320, both on the same PC. Logs are `Saved/Logs/codex-packaged0803-lan-{host,client1}-20260908.log`. All menu, card, and lock actions used physical Computer Use input. Bots supplied natural goals; no forced scores, console travel, or runtime Python was used in these packaged processes.

- Initial host flow: Play Lyra → Start a Game → LAN / Bots Enabled → Local / Listen Server + Bots → Argus → Lock Selection. Client discovered the 1/8 LAN entry and joined during the match. It played as the default Argus; no initial manual client selection is claimed. Both processes reached Team 2's 1–3 result across four rounds at 15:34:28 UTC, with both human columns showing Argus.
- Remote Play Again requested shared staging at 15:37:26.615. The sole selection start was 15:37:30.685. The remote player changed to Alona and locked at 15:37:59.493; the returning host changed to Korryn and locked at 15:38:27.204, 3.481 seconds before the deadline. The second lock immediately caused the sole arena travel. There was no timeout fallback. Host HUD showed 335 health and six Korryn slots; client HUD showed 350 health and six Alona slots. Server restart logs identify both selected heroes and their ability sets.
- This rematch completed naturally, 0–3 over three rounds, at 15:39:52.343 on the host. Both rendered breakdowns matched all eight visible columns: human Korryn/Alona entries, 63 gold per Team 1 player, 363 per Team 2 player, three credited goals, and the same Korryn MVP. This low-combat sample does not establish nonzero damage/healing/interception field coverage. Both screens still showed the known waiting banner behind results, expected because 0803 predates f50104ec.
- Host Return To Lobby initiated server travel to the listen frontend at 15:40:27.147. Both processes visibly reached the frontend. The remote player opened Browse and saw no joinable games. The host retained LAN/Bots Enabled settings and selected the same local playlist. The remote player automatically followed into staging from its browser screen: no refresh, second join click, process restart, or manual reconnect was needed. This proves connected group travel; it does not prove discovery of a freshly advertised session by a disconnected client.
- The second selection started once at 15:42:27.902. Remote Alona → Rawlins locked at 15:42:58.961; host Korryn → Argus locked at 15:43:23.619, 4.283 seconds before timeout. One immediate arena travel followed. Physical HUD observations showed Argus at 500 health and Rawlins at 400, each with six populated slots. The rehosted match completed naturally, 0–3 over three rounds, at 15:45:33.933 (host) and 15:45:33.941 (client). Both rendered breakdowns matched: human Argus/Rawlins, 109/409 gold per player by team, MVP Argus with two goals, and one Rawlins goal.

Remaining presentation observations: oversized/floating placeholder meshes, missing animation Blueprint warnings/T-poses, debug text, relic-request dynamic-material warning, and the known waiting banner. The remote's reactivated selection initially displayed its old Alona heading/portrait with the generic choose-a-hero detail prompt; clicking Rawlins populated the correct details and allowed locking. Selection functionality passed; full card presentation polish remains open.

Attempt accounting for this packaged gate: reused one candidate, one preserved host plus one client launch, one manual rematch and one shared frontend rehost, zero new builds/packages. Source/asset fixes newer than c5e11663 are not validated by these results. Four-player target topology, second-PC LAN, disconnected-client rediscovery, physical all-kit/gamepad actions, purchased-upgrade resets, and final placeholder integration remain open. Invalidate affected evidence after session, selection, ownership, or relevant HUD changes.

Both instances were closed with normal window-close input after final results. Their processes exited; each log contains normal `LogExit: Exiting.`. Final compact inventory: `Saved/Logs/codex-packaged0803-manual-flow-20260908.json`. Across the full host log, exactly three selection starts and three arena travels cover initial selection, Play Again, and frontend rehost. Both logs have three results deliveries and zero error/critical lines. The helper is a log inventory; physical observations above supply the UI/selection evidence. No new runtime fix was made during this gate.

### Optional footstep Niagara spawn — bcec5a3e

The four-peer NullRHI bot test had produced 10,415 Blueprint error lines against `B_FootStep.NS_FootPrint`. The graph validated an existing component before reuse, but assumed `SpawnSystemAttached` always created a replacement. A null return went directly into five Niagara parameter writes. Engine source confirms that Niagara component creation requires rendering and excludes dedicated servers; spawn preculling can also suppress creation.

Changed only `Content/Effects/Blueprints/B_FootStep.uasset`: after assigning the spawn result, an IsValid call and Branch admit only a valid component to trigger initialization and the existing parameter-write chain. A failed spawn returns from that branch while the event's independent lifetime sequence still runs. Existing reuse, surface selection, parameters, effect template, auto-destroy, pooling, and delay behavior remain intact. The asset was initially clean; only it was saved and committed as `bcec5a3e`. No C++ change or editor rebuild was needed for this fix.

Evidence under `Saved/Logs/footstep-audit-20260908/`: before/after/cold T3D exports and `graph-diff.json`. The event graph added two nodes, removed none, and changed the two intended setter connections. The existing delay's Completed pin acquired a localized label during save. Raw cold export comparison differed in that label representation and an unconnected hidden ErrorTolerance pin ID; after normalizing only those inspected metadata differences, all semantic records matched. Validation passed before and after cold restart with zero errors and warnings. `Saved/inspect-footstep-20260908.py` and `compare-footstep-graphs-20260908.ps1` preserve the inspection.

- NullRHI four-peer Dorado: `codex-footstep-nullrhi-fixed-20260908.json` passed 161 samples over 40.016 seconds, observing up to 21 footstep actors and zero Niagara components. No Blueprint runtime errors remained. `codex-bot-ownership-footstep-nullrhi-fixed-20260908.json` also passed a natural two-round match with four humans/four bots and matching eight-player rosters; its observer ran 26.688 seconds.
- Rendered four-peer Dorado after a cold editor restart: `codex-footstep-rendered-fixed-20260908.json` passed 157 samples over 40.188 seconds, up to 24 valid active Niagara components, and 338 trigger changes on reused components. `codex-footstep-rendered-cleanup-20260908.json` found zero footstep actors in all four worlds after results settled. The natural two-round roster observer passed in 13.219 seconds; this is observer duration, not total match duration. No Blueprint/Python/ensure/assertion/fatal errors appeared in the rendered log. Host and Client 1 results were visually inspected without the earlier waiting banner, providing supplemental rendered evidence for f50104ec. Particle artwork quality was not assessed from the postmatch images.

Runtime logs: `codex-footstep-nullrhi-ready-20260908.log` and `codex-footstep-rendered-20260908.log`; counts in `codex-footstep-log-summary-20260908.json`. The NullRHI log retains two pre-test Python probe failures: Blueprint.UbergraphPages is protected. Native bridge node-property operations completed the layout; no world wrappers were retained by those asset-only failures. `Saved/verify-footstep-20260908.py` kept runtime wrappers local and recorded scalar snapshots only. Both observer callbacks completed before PIE stopped.

Attempt accounting: three editor launches, including one failed sandbox-only launch with no writable DDC nodes. Failed PID 26620 exited before the authorized launch; no cache/security settings were changed. Successful NullRHI editor 19604 and rendered editor 58376 each ran one four-peer PIE match and exited normally at 16:01:14 and 16:07:04 UTC. Dirty packages were empty before each quit. Reflected settings restoration did not persist the empty extra-server URL across the first restart; after the final editor exit, the single captured test URL was removed from EditorPerProjectUserSettings.ini. On-disk verification confirmed four clients, one process, listen-server mode, and empty AdditionalServerGameOptions. DevMap and the original full-flow flag were restored. BF-113 records prevention.

Scope: same-process four-peer PIE component creation/reuse/cleanup and natural match state. Packaged, separate-process, second-PC, and final VFX/art acceptance remain separate.

### Candidate 0804 combined fixes and packaged headless check

One BuildCookRun attempt completed successfully in 179.04 seconds, AutomationTool exit 0. Source `bcec5a3efddaaa85ff309a8568c291edfb9c0f10` contains f50104ec, 70bea97c, and the footstep fix. Project/engine HEAD, complete tracked patches, status, generated chunk-layer input, logs, exit, and the artifact hash manifest are preserved under `Saved/Logs/codex-packaged-LAN-20260908-04-*`; pipeline `Saved/package-LAN-20260908-04.ps1`. The live packaging exec session 91103 completed. A transient Zen oplog connection failure recovered inside this same UAT attempt; no second build or configuration change was required. The existing absent Content/DTLS staging-directory warning remains.

Artifact: `Saved/StagedBuilds/LAN-20260908-04/Windows/ProjectB/Binaries/Win64/LyraGame.exe`, SHA-256 `66FFCC527AAA53FE5541D7361C24F8959EE9DAC21AC975423A1E8776F39216C4`, rehashed and matched to its manifest. The stage contains 131 manifest files totaling 4,257,298,125 bytes, including retained local debug files. This verifies artifact identity, not the four-player LAN route.

One packaged NullRHI process (PID 66024) entered Dorado/CaptureTheRelic directly with selection skipped, Argus, NumBots=7, PointsToWin=2, 30-second rounds, three-second warmup, and two-second PostRound. It reached 0–2 across two natural rounds at 16:15:22.108 UTC, with zero runtime error/critical lines. Evidence: `codex-packaged0804-headless-20260908.log` and `codex-packaged0804-headless-flow-20260908.json`. The native test-exit trigger matched the real PostMatch line and requested forced exit with status 0; it does not prove rendered results, graceful frontend return, or multiplayer transport. The process was confirmed absent afterward.

The initial PowerShell GUI invocation returned before the game ended. By the time a process handle was requested for OS exit capture, PID 66024 had exited. The exit record explicitly says unavailable; do not substitute the shell's zero status for an observed game exit code. No duplicate gameplay test was launched just to recapture that value. Future GUI launches should use Start-Process -PassThru, retain the process handle, and capture its exit. Runtime source/asset checks and the completed UAT exit remain independent evidence.

Next: launch the exact 0804 artifact through the LAN frontend; verify natural results without the waiting banner on both peers and continue toward the required one-PIE-plus-three-process topology. Keep disconnected discovery, second-PC LAN, gamepad/all-kit actions, upgrade reset, full buildables, visual placeholder integration, and performance gates open.

### Candidate 0804 rendered LAN results and shared return

Expected result: a disconnected packaged client discovers the host through the LAN browser, joins the match, and receives the same natural results without the obsolete waiting banner; both can return to the frontend. Reused the verified 0804 hash above, with no runtime source/assets/config changes since its recorded source. Host PID 44220 and client PID 47976 were launched once each with retained process handles. All menu, hero-card, lock, and close actions used physical Computer Use; bots supplied natural goals. No forced scores or console travel were used.

The first host-only match timed out selection with Rawlins and completed naturally at 16:26:02.403 UTC, 0–3 over three rounds. Its results had no waiting banner. The client remained in its browser and did not participate; this first run supplies host-only evidence. Host returned to the frontend at 16:28:08.677 and created another LAN / Bots Enabled / Local Listen Server + Bots session.

The previously disconnected client refreshed Browse, saw the correctly named 1/8 LAN entry with 7 ms ping, and joined during staging. It selected Rawlins and physically locked at 16:30:12.136, approximately 0.77 seconds before the host selection timeout. The host retained Rawlins and was force-locked by timeout at 16:30:12.906; do not claim two manual locks. Both then showed 400 health and six populated Rawlins ability slots. This second session reached natural 0–3 results at 16:31:51.731 on the host and 16:31:51.746 on the client.

Both rendered breakdowns matched all eight visible columns: Team 1 Korryn/Alona/Rawlins/Rawlins and Team 2 Alona/Alona/Korryn/Alona; 78 gold per Team 1 player and 378 per Team 2 player; two goals for the first Team 2 Alona (the shared MVP) and one for the second. Other displayed statistics were zero. Neither screen showed the waiting banner. The host's team interstitial showed the corresponding 312/1,512 gold totals. This sample does not verify nonzero combat statistics or every result field under combat.

Host Return To Lobby initiated shared frontend travel at 16:32:18.825. Both windows visibly reached the frontend. Client and host were then closed normally; retained monitors captured actual OS exit 0 for each. Both PIDs are absent. Logs `Saved/Logs/codex-packaged0804-lan-{host,client1}-20260908.log` and final `codex-packaged0804-rendered-flow-20260908.json` contain zero runtime error/critical lines and normal exit markers. Whole-file inventory: two host selection starts/travels/results, one client results delivery. This gate used two game launches, two natural matches (first host-only), zero editor launches, and zero new builds/packages.

The phase/footstep fixes now have rendered packaged evidence for this scope. This is two processes on one PC, not the required mixed four-player or second-PC acceptance. No repeat of the earlier manual-rematch gate was needed. Outstanding observed polish: floating/offset meshes and missing animation Blueprint warnings, visible developer/sample menu entries and debug text, virtual-shadow-map overflow, relic-request material warnings, missing `GameplayCue.Breakaway.Movement.Slide` registration for `GCN_BW_SlideTrail`, and an invalid-handle unregister warning on remote frontend return. Next inspect the authored hero mesh transforms, skeleton/animation assignments, and runtime attachment before fixing placeholder integration. Invalidate affected evidence after relevant source, asset, or session changes.
